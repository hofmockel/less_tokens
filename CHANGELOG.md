# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Fixed
- **Savings HTML doctor added for the stale/empty page investigation (BACKLOG Research why `savings.html` appears to show nothing)** — `stats.py --doctor-html` now prints the active agent/state dir, `savings.jsonl` path, event count, malformed-line count, newest event timestamp, `savings.html` path/mtime, session versus all-time record counts, measured versus upper-bound totals, and whether the `savings-html.py` hook refresh succeeds from both the repo root and a nested cwd. This turns the visible "nothing showed up" symptom into a reproducible diagnostic that distinguishes missing logs, wrong agent path, stale HTML, session filtering, measured-vs-upper-bound confusion, and cwd-sensitive hook failures.
- **Installed budget config copy no longer references deleted review notes** — synced `.less_tokens/hooks/budget/config.py` with `agents/common/budget/config.py` so the `effective_mode()` docstring no longer points at the removed `eb_plan_4jul26.md` file. Cosmetic-only bug from a generated installed-copy drift; behavior unchanged.
- **Codex hook commands now run from nested working directories** — `.codex/hooks.json` entries now use absolute paths for both the `.less_tokens/bin/python` launcher and the `.codex/hooks/<script>.py` adapter, while Claude hook commands stay relative to match Claude Code's project-root cwd behavior. Previously every Codex hook command was emitted as `LESS_TOKENS_AGENT=codex .less_tokens/bin/python .codex/hooks/...`, so any Codex tool call launched from a subdirectory failed before the hook script started (`.less_tokens/bin/python: No such file or directory`) and silently disabled all 16 less_tokens Codex hooks for that call. Added a regression test that executes a generated Codex hook command from a nested cwd with a fake launcher/hook, plus updated install/check coverage for absolute hook commands. Closes the `savings.html` stale-regeneration duplicate noted in BACKLOG.md; the separate `savings.html` UX research item remains for measured-vs-upper-bound/session/path confusion.
- **`truncate-output.py` now actually reduces what Claude sees, instead of a false-positive PostToolUse message** — verified against `code.claude.com/docs/en/hooks.md`: `PostToolUse` exit code 2 only surfaces stderr to Claude (the tool already ran); it never substitutes stdout for the delivered tool result. The hook was printing the truncated text to stdout and exiting 2, so the model always received the full, untruncated output while the hook's `[truncated — N chars omitted]` stderr message falsely claimed content had been elided — confirmed live: a large `cat` returned complete content in the same turn's tool result despite the hook's omission claim. Fixed `.claude/hooks/truncate-output.py`'s `main()` to emit `hookSpecificOutput.updatedToolOutput` (the documented mechanism to replace a `PostToolUse` tool result) plus `additionalContext` for the notice, and exit 0. Scoped to the Claude entrypoint only; the Codex hook protocol wasn't verified against this same mechanism. Regression tests: `test_truncate_output_updated_tool_output.py` (new); `test_hooks_protocol.py::TestTruncateOutput` updated to assert the JSON contract instead of the old exit-2/plain-stdout one. Filed from `ever_better`'s `BACKLOG.md`.
- **`context-cache` no longer counts a search-first-denied Read as "in context"** — `check_context_cache` (`agents/common/hooks/context_cache.py`) called `record_read`/`record_grep` unconditionally on every `PreToolUse` pass, with no visibility into whether a *sibling* `PreToolUse` hook (`search-first.py`) was about to deny the same call. A denied Read's content is never delivered, but context-cache still marked it served, so the mandated follow-up (search, then retry) got wrongly blocked as "already in context" — a fail-misleading state. Fixed to mirror the existing Bash pattern: Read/Grep now only `check_*` on `PreToolUse` and only `record_*` on `PostToolUse`, which never fires for a call a sibling hook denied. Wiring updated in `agents/common/hooks/hook_manifest.py` (`context-cache` gains a `PostToolUse Read|Grep` wire for Claude, `PostToolUse mcp__filesystem__.*` for Codex) and regenerated into `README.md`/`DOCUMENTATION.md`'s parity tables; the Claude entrypoint (`.claude/hooks/context-cache.py`) now threads `hook_event_name` through to `check_context_cache` (it previously always defaulted to `PreToolUse`, same as Codex's entrypoint already did). Regression tests: `test_denied_read_is_not_recorded_as_served` (new, both `.claude/tests/unit/test_context_cache.py` and `test_codex_hooks.py`); updated the two existing Read tests that assumed a single `PreToolUse` call auto-records, and the hardcoded Claude hook-count assertions in `test_install_hook_entries.py` (20→21). Filed from `ever_better`'s `BACKLOG.md`, reproduced there as a strict `xfail`.
- **`near_misses.jsonl` test contamination fixed; log added to the CI isolation gate** — `record_session_size_sample` (`agents/common/hooks/compact_trigger.py`) and `_record_search_near_miss` (`.claude/tools/search.py`) both resolve their write directory via `active_state_dir()`, but two compact-trigger tests (`test_bug_compact_trigger_stale_last.py`) only monkeypatched `STATE_FILE`/`MAX_SESSION_CHARS`, `test_hooks_protocol.py`'s `TestCompactTrigger` ran the hook as a subprocess with no `LESS_TOKENS_STATE_DIR` override, and `test_agent_model.py`'s two `search_mod.main()` calls left `active_state_dir` untouched — all three real code paths (`main()` in `.claude/hooks/compact-trigger.py`, the protocol-test subprocess, and `search.py`'s CLI) wrote real lines into the repo's actual `.claude/state/near_misses.jsonl` on every test run, seeding it with round-number fixture sizes (`600_000`×N, `650_000`×N) that don't reflect real session sizes — this is what BACKLOG.md's de-noise item's ~25%-fixture estimate was describing. Fixed by isolating `active_state_dir()`/`LESS_TOKENS_STATE_DIR` in all five call sites across the three files. `near_misses.jsonl` added to `conftest.py`'s `_no_production_telemetry_writes` regression net (previously only watched `events.jsonl`) so this contamination class fails CI if it recurs. Full unit + integration suites re-run clean with zero growth in the real `near_misses.jsonl` (827 + 87 passed). Threshold-height decision itself (BACKLOG.md, informed by `eb_telemetry_9jul26.md` §1) is unaffected by this fix and still open — this only stops the dataset from being re-contaminated by CI going forward.
- **G17 Claude spawn/no-spawn heuristic now documented** — the new Claude `less-tokens` skill (`agents/claude/skills/less-tokens/SKILL.md`) states the break-even rule: a spawned Agent-tool child pays a full fixed startup tax (system prompt + CLAUDE.md + tool schemas) with no transcript copy, so only spawn when the discarded child context would exceed that tax; skip spawning for a single Read/Grep, one small file check, or a short test command. Mirrors the existing Codex-side G17 guidance.
- **G16 Claude per-agent fixed bucket now has narrow agent-definition files** — added `.claude/agents/explorer.md` (Read/Grep/Glob only) and `.claude/agents/verifier.md` (Bash/Read only), installed via a new `agents/claude/agents/` copy step in `install.py`. A narrower `tools:` allowlist means fewer tool schemas loaded per spawned child than the generic `general-purpose` agent — the per-child fixed-cost lever this item asked for. Codex side was already satisfied (tiny `AGENTS.md`, break-even guidance already in the Codex skill).
- **G14 Claude dead-end exploration quarantine now documented** — the Claude `less-tokens` skill and `.claude/agents/explorer.md` direct messy "find where X is wired" searches to the read-only `explorer` agent so dead ends and false starts stay in the child's window; only the conclusion returns. Mirrors Codex's G11.
- **G15 Claude SubagentStop wiring shipped; Codex-side nested-cwd bug found and deferred** — `terse-output`/`caveman-reminder.py` and `savings-html` now also wire `SubagentStop` in `agents/common/hooks/hook_manifest.py` (both scripts already read `transcript_path`/`stop_hook_active` off stdin without inspecting `hook_event_name`, so no script changes were needed), closing the gap where those two Stop-only levers didn't reach a Claude subagent's own transcript. `.claude/settings.json` updated to match; `test_subagent_stop_wired_for_terse_and_savings` (`.claude/tests/unit/test_install_hook_entries.py`) pins it. While building the Codex-side acceptance test (nested-cwd hook invocation), found a real bug: `.codex/hooks.json` commands use a relative path (`.less_tokens/bin/python ...`), which resolves from the repo root but fails (`exit 127`, silent) when invoked from any other cwd — confirmed by installing into a scratch repo and running the installed `savings-html.py` command from a nested subdirectory. Filed to `BACKLOG.md`'s Bugs table for Codex to fix in its own context (absolute-path Codex hook commands vs. Claude, which stays relative because the harness resets Bash cwd to project root between tool calls).
- **New Claude-side `less-tokens` skill, mirroring the Codex one** — `agents/claude/skills/less-tokens/SKILL.md`, installed to `.claude/skills/less-tokens/` via a new copy step in `install.py` (`_install_specs()` and the real install routine both updated; uninstall pruning and the foreign-file shared-dir exemption updated to match). Covers search/symbols/read-guard/claudemd-audit/embeddings commands plus the G14/G16/G17 delegation guidance above. Tests: `.claude/tests/unit/test_claude_skill_install.py`.
- **G12 Codex large-source summarization now has a digest-path prompt contract** — the Codex `less-tokens` skill now tells spawned agents to summarize large docs/logs into `.less_tokens/state/<name>.md`, return only the digest path, source path, line references, verification, and blockers, and avoid making the parent read the source first.
- **G11 Codex noisy-verification delegation now has a compact prompt contract** — the Codex `less-tokens` skill now includes a verification-subagent template that keeps test/lint/build logs in the child and returns only pass/fail, failing command, top cause, file-line references, and blockers, with an explicit warning to keep short checks local unless the parent can continue non-overlapping work.
- **G8/G9 Codex subagent guidance now covers parallel dispatch and output contracts** — the Codex `less-tokens` skill now gives a short checklist for independent parallel subtasks, `explorer` versus `worker` use, disjoint file ownership, non-overlapping parent work, agent closure, and a strict four-field return contract with file-line findings instead of pasted bodies/logs/diffs.
- **G7/G13 Codex context packs now have a pointer-only prompt template** — the Codex `less-tokens` skill now gives a compact context-pack shape for spawned agents: objective, search command, `path:line` pointers, slice-only reads, and file-line findings. This ships the Codex-owned guidance for subagent context re-derivation and pointer-not-payload spawn prompts without claiming automatic Codex subagent behavior.
- **CX15 — Codex spawn break-even guidance now reflects a manual fork-context comparison** — a parent-only search found the answer with one targeted `rg`; the cold `fork_context=false` explorer returned the same compact finding from pointers; the full-history fork required the supported inherited-agent spawn shape and did extra semantic-search plus `rg` discovery. The Codex `less-tokens` skill now says to reserve `fork_context=true` for children that truly need prior conversation.
- **CX13 — Codex install checks now smoke-test hook wrappers from a nested cwd** — `install.py --check --agent codex` runs representative installed `.codex/hooks/` wrappers through `.less_tokens/bin/python` from a nested project directory with `LESS_TOKENS_AGENT=codex`, so missing launchers, hook files, shared imports, or cwd-sensitive repo resolution fail with actionable diagnostics instead of relying on a live Codex subagent.
- **README no longer overstates installed behavior for `/def`, Codex hook wiring, and `.claude/` artifact ownership** — the "Vector search + symbols" row claimed `/def` is an installed slash command, but `install.py`'s `_install_specs()` never deploys `.claude/commands/`; reworded to describe the underlying `symbols.py` lookup and note `/def` only applies when slash commands are installed. The "wired by default" sentence and the Quick start artifact-location paragraph implied Codex hook wiring and `.claude/` ownership are unconditional, when `.codex/hooks.json` wiring is skipped and falls back to `AGENTS.md` + skills whenever `.codex/` isn't writable, and `.claude/` holds shared implementation/index artifacts used by both agents, not Claude-only files. The compaction-trigger row said the hook nudges `/compact` for both agents, but Codex has no slash-command path and instead nudges a fresh or compacted Codex session (`agents/codex/hooks/compact-trigger.py`). Fixes filed in `BACKLOG.md`'s README Accuracy section.
- **context-cache no longer trusts mtime alone as proof a file is unchanged** (`agents/common/hooks/context_cache.py`) — `check_read`/`record_read` now also track `st_size` and require both mtime and size to match before serving a cached "file unchanged" response. Previously a same-mtime write that changed content (`cp -p`, `git checkout`/revert, `rsync --times`, coarse fs mtime granularity) was served as unchanged, letting a downstream `Edit` match its guessed `old_string` against unrelated text and silently apply against stale context. Found in cross-repo team review from `ever_better` (2026-07-04, `tect` + `qa` independently confirmed), filed to `BACKLOG.md`'s Bugs table, fixed here. Regression test: `test_read_same_mtime_different_size_not_blocked` (`.claude/tests/unit/test_context_cache.py`) — confirmed failing against the pre-fix code, passing after.
- **v2 budget-plane telemetry no longer leaks into production `events.jsonl`** — `events_path(root)` (`agents/common/budget/events.py`) derived `.less_tokens/state` directly from the `repo` path threaded through `budget_hook_outcome`, never consulting `LESS_TOKENS_STATE_DIR`. Every contract test that set `LESS_TOKENS_REPO=<real repo>` (needed to bootstrap hook imports) wrote real telemetry lines into the actual repo's `.less_tokens/state/events.jsonl` — 160/160 lines in that file were pytest artifacts (`session_id: "contract-session"`), meaning the plane had never recorded a single real production event. Added `resolve_state_root()` (`agents/common/budget/state.py`), mirroring `search_config.active_state_dir()`'s override-then-fallback pattern; `budget_hook_outcome` now resolves it once and threads it into `evaluate_budget_input`/`should_emit_advice`, while `load_budget_config` keeps reading the real repo (config is a repo artifact, not per-session state). Also fixed the same class of bug in the manual CLI tools `.less_tokens/tools/budget_report.py`/`budget_doctor.py`, which called `load_events(REPO, ...)` directly. Added a CI-blocking regression test (`test_hooks_state_dir.py::TestBudgetObserverStateDirIsolation`, both Claude and Codex) and a general `_no_production_telemetry_writes` session fixture (`.claude/tests/conftest.py`) that fails the whole suite if `.less_tokens/state/events.jsonl` grows during a test run, catching this bug class for any future hook.
- **Event registry: enforce emit/consume parity for stats keys** — `context-cache-bash` (emitted by `context_cache.py`'s Bash-repeat block, added alongside `context-cache-read`/`-grep`) was never added to `stats.py`'s `_STRATEGY_LABELS`/`_MEASURED_STRATEGIES`, so `_summarize`'s `if s in data` guard silently dropped every cached-bash saving from the report — the same class of bug fixed once before for `context-cache-read`/`-grep`. (`context-cache-read`, `context-cache-grep`, and `glob-cap` were already correctly registered; they were not the gap.) Added a canonical `_KNOWN_STRATEGIES` registry (key → label, basis) to `savings_log.py` as the single source of truth; `stats.py` now derives `_STRATEGY_LABELS`, `_MEASURED_STRATEGIES`, and `_UPPER_BOUND_STRATEGIES` from it instead of hand-maintaining a second list. Every static `strategy=` call site under `.claude/hooks/`, `.claude/tools/`, `agents/common/hooks/`, and `agents/codex/hooks/` now writes a `STRATEGY_*` constant instead of a bare string literal. Added `.claude/tests/unit/test_event_registry_parity.py` (static source-scan: every emitted literal/constant must be a registered key) and a coverage assertion in `test_stats.py` (every registered key must have a label and land in exactly one basis bucket) — both are exercised by CI's existing `pytest .claude/tests/unit/ -v` step.
- **Terse-output mode no longer clips a user-requested document draft** — `caveman-reminder.py` and `terse-reminder.py` scored *any* unfenced chat-reply prose against the word cap and filler-phrase list, including a README section, report, or proposal the user explicitly asked for pasted directly in the reply (content routed through `Write`/`Edit` or wrapped in a code fence was already exempt). Added a literal sentinel, `<!-- less-tokens: document-draft -->`, checked first in `response_budget.analyze()`: when present anywhere in the text, the response is fully exempt with no further scoring. The assistant sets it only because the *user's* message asked for a document — never on its own judgment of length or importance, avoiding a self-grading exemption. Documented in `.claude/rules/caveman.md` (Claude, ships via `install.py`'s caveman block) and `agents/codex/instructions/AGENTS.md.fragment` + the `less-tokens` skill (Codex). Spec: `ever_better` repo, `reports/runs/2026-07-02-less-tokens-onboarding/SPEC-terse-mode-doc-exemption.md` (approved by hofmockel 2026-07-02).
- **Generated install output no longer committed into the toolkit repo** — a stray commit had checked in `install.py`'s generated output (most of the `.less_tokens/` tree and all of `.codex/`). The committed `.less_tokens/bin/python` shim `exec`s `.claude/.venv-tokens/bin/python`, which CI never creates; because the codex `lean-output.py` hook prefers that shim when present, `test_lean_output_parses_pytest_failure` got empty stdout and failed across the whole `tests.yml` 3×3 matrix. Untracked the generated files and tightened `.gitignore` to ignore the generated `.less_tokens/`/`.codex/` paths while still tracking the three budget control-plane source files install distributes (`.less_tokens/config/budget.json`, `.less_tokens/tools/budget_doctor.py`, `.less_tokens/tools/budget_report.py`) — the `test_budget_doctor_smoke` / `test_budget_report_includes_risk_and_compactions` subprocess tests depend on those. CI now falls back to `sys.executable` for the hook and the budget tools resolve.
- **`stats.py` console output no longer crashes on non-UTF-8 terminals** — the report table prints `≤` and em-dash glyphs, which raised `UnicodeEncodeError` on a Windows cp1252 console (`print` → exit 1). `main` now reconfigures stdout/stderr to UTF-8 up front. Surfaced by the new cross-platform integration test (the old ubuntu-only CI job never hit it); the report *file* was already UTF-8, only console printing was affected.
- **Cached partial-read sizing streams the slice** — `context_cache.py`'s `blocked_read_chars` no longer loads the whole file via `read_text().splitlines()` just to size an `offset`/`limit` slice; it streams the file and stops after the requested line range, keeping the block path cheap for large files. (Codex review, PR #55.)
- **Savings-report smoke is now a real integration test** — the `Generate savings report` CI job was failing (`stats.py: error: unrecognized arguments: --enable`; the enable/disable flags were removed in Phase 1) and only asserted non-crash. Replaced it with `.claude/tests/integration/test_stats_report.py`, which seeds synthetic events through `savings_log`, runs `stats.py --report` as a subprocess, and asserts on the rendered report (measured vs upper-bound panels, row aggregation). It runs in the `tests.yml` 3×3 matrix; the bespoke `stats-report` job is deleted.

### Changed
- **Codex hook wrappers now share their adapter boilerplate** — added `agents/codex/hooks/_codex_runtime.py` for repo discovery, Codex path setup, stdin JSON loading, filesystem read/search mapping, and stdout/stderr result printing. All Codex hook scripts now use that helper instead of local `_resolve_repo()` blocks, direct `json.loads(sys.stdin.read())`, or repeated read-tool mappers; the larger `search-first`, `truncate-output`, `compact-trigger`, and `index-refresh` wrappers now do work inside `main()` instead of at import time. Added an install regression that verifies `_codex_runtime.py` is copied into `.codex/hooks/`.
- **Context-cache savings now appear in the report** — the `context-cache.py` hook (repeat Read/Grep blocker) already wrote savings events to `savings.jsonl`, but `stats.py` knew only four strategies and silently dropped them in `_summarize`. Added `context-cache-read` and `context-cache-grep` to the strategy labels and the **measured** panel (a blocked duplicate Read is exact, not a counterfactual — the agent issued the Read with known args and those bytes were prevented from re-entering context; the grep row counts events only since the grep never ran). The emitter now writes the full Phase-1 schema (`basis="measured"`, `kept_chars=0`, `elided_chars`, `content_kind`, `where`, resolved `session_id`/`session_source`) instead of the bare legacy shape, and the legacy-tolerant loader classifies pre-existing context-cache records as measured. `elided_chars` credits only what would have re-entered context — the full file for a whole-file read, or just the line slice for a partial read — instead of always crediting full file size. Methodology prose updated.
- **Savings report separates measured from upper-bound and groups by real session** — Phase 3 of the stats framework rewrite (`stats_plan.md`). `stats.py --report` (and the terminal print) now render two independent panels per block: **Measured** (truncation, compaction — actually removed before reaching the model) and **Upper bound** (search-first block, search — counterfactual avoided cost, each magnitude prefixed `≤`). The two carry separate totals and are never cross-summed, so optimistic search estimates can no longer inflate the real-savings number. The "session" view now groups by the resolved `session_id` (current session = the most recent real-session bucket); the old 8h wall-clock window survives only as a labeled legacy fallback for records with no real session. The token footer is now the honest `tokens est. at chars÷4 (uncalibrated)` and stays uncalibrated until Phase 6 `--calibrate`. Removed the `--disable` usage line from the CLI help (tracking is always-on).
- **Savings tracking is always on, local-only, with a new exact-chars schema** — Phase 1 of the stats framework rewrite (`stats_plan.md`). Dropped the `TRACK_SAVINGS` opt-in gate (and the enable/disable CLI + interactive prompt); tracking now runs from the first session of every install and writes only to the active local state directory (`.claude/state/savings.jsonl` for Claude, `.less_tokens/state/savings.jsonl` for Codex), never transmitted. Disable with the `LESS_TOKENS_NO_STATS=1` environment variable. Each event now records exact `kept_chars`/`elided_chars` plus `basis` (`measured` vs `upper_bound`), `content_kind`, `where`, and a resolved `session_id`/`session_source` (payload → transcript-hash → env → `local-session`); the search-first block also emits a `correlation_id`. `truncate-output.py`, `search-first.py`, and `search.py` emit the new shape; `stats.py` gained a legacy-tolerant loader that maps pre-Phase-1 records (`saved_chars` → `elided_chars`, infers `basis`, tags `content_kind="legacy"`, folds `glob-cap` into `truncation`) without rewriting the file. Tokens remain a report-time estimate, never stored.
- **Codex stats parity completed** — Codex adapter hooks now emit the same savings records as Claude for truncation, search-first blocks, and measured compaction, and the savings logger/report reader use the agent-aware active state directory (`.less_tokens/state` for Codex, `.claude/state` for Claude). Added subprocess tests that verify Codex records are written for those hook interventions.

### Removed
- **`_set_tracking` type-hint regression test** — deleted `test_bug26_set_tracking_type_hints.py`; the `_set_tracking` helper it guarded was removed with the `TRACK_SAVINGS` gate (always-on tracking has no enable flag to flip).
- **Fixture-driven perf benchmark** — deleted `.claude/tests/perf/` (`test_bench_tokens.py`), the `perf` pytest marker, and the `Perf benchmarks` CI job. Its `reduction >= MIN_*_REDUCTION` assertions measured properties of hand-built fixtures, not real-session savings. Phase 0 of the stats framework rewrite (`stats_plan.md`): pure removal with no replacement dependency, done first so it stops gating the rewrite on a contract the rewrite deletes.

### Added
- **CX12/CX14 — Codex delegated-work guidance and support boundary** — added a compact optional Codex subagent prompt template to `agents/codex/skills/less-tokens/SKILL.md` and documented the boundary in `DOCUMENTATION.md`: subagents are a user-authorized Codex app surface, not automatic `less_tokens` hook behavior; `.codex/hooks.json` cannot reroute parent reasoning into children. The skill now covers `fork_context=false`, pointer-not-payload prompts, `explorer` vs `worker`, disjoint ownership, compact return fields, and spawn/no-spawn break-even guidance.
- **Per-category budget mode overrides; `unscored_context` flipped to advise** — `BudgetConfig` gained `category_modes: dict[str, str]` (default `{}`, fully backward compatible) and an `effective_mode(category)` method, so a single narrow category can move to `advise`/`enforce` without changing the global `mode` or any other category's behavior. The `unscored_context` hard-cap check (`gate.py`'s `_forced_decision`, previously hardcoded to fire only in `strict` mode) now uses its own `effective_mode` and tags its decision with `category="unscored_context"` explicitly (previously it inherited the candidate's original category, which never matches any per-category key). `advice.py`'s `advice_for_mode`/`enforcement_decision`/`outcome_for_mode` now filter decisions by each one's own effective mode. The advice rate-limit gate in `budget_observer.py` (previously gated on `config.mode == "advise"` specifically) now fires on any advisory output regardless of source, so category-driven advice can't bypass rate-limiting. Live config: `"category_modes": {"unscored_context": "advise"}`; global `mode` stays `observe`.
- **`stats.py --audit-liveness`** — a per-strategy liveness check against real `savings.jsonl` telemetry: is each shipped strategy wired, has it fired recently, and is its firing rate consistent with an `expected_frequency_class` (`frequent`/`rare-but-real`/`experimental`) recorded once per strategy. Flags a `frequent` strategy with zero events over the window (default 90 days) as a dead lever worth investigating, while a `rare-but-real` strategy (compaction) is never flagged regardless of age. Manual/periodic command, not a CI gate — CI has no accumulated production telemetry to check against, and asserting one would mean fabricating fixture data, which `stats_plan.md` already bans. Unit tests cover the classification logic against hand-built fixtures only.
- **`label_consistency_gate.py`** — new CI/pre-commit gate modeled on `changelog_gate.py`: scans `README.md`'s strategy table for percentage/magnitude savings claims and cross-references each against the `savings_log._KNOWN_STRATEGIES` registry. A claim with no registry-backed strategy and no explicit "not telemetry-backed"-style marker fails the build, naming the row. Fixed the two claims that would have failed it today (`Terse output mode`, `Lean tool output` — both real hooks with no savings.jsonl category by design) by adding the marker to their README rows. Wired into `.github/workflows/tests.yml` and `.pre-commit-config.yaml` alongside `changelog-gate`.
- **Near-miss instrumentation for cached-bash/cached-grep and the compaction trigger** — additive-only telemetry (`near_misses.jsonl`), never read by any hook, never changes behavior. `context_cache.py` logs a coarse signature (`command`'s first token, or the grep pattern) whenever a Bash/Grep call misses the exact-string cache — real data on what's actually being repeated, ahead of deciding how to widen `cacheable_bash_command()`/`grep_key()` (both currently 0 events in all-time telemetry). `compact_trigger.py` logs transcript size on every invocation, not just when the trigger fires — compaction has fired once in this repo's entire recorded history, and there was no data on how close other sessions come to `MAX_SESSION_CHARS` without crossing it.
- **Near-miss instrumentation for `search.py`'s repeated-query question** — same additive-only, fail-open `near_misses.jsonl` discipline, applied to the still-open "same-session `search.py` repeated-query cache" backlog item: `_record_search_near_miss()` appends a `kind: "search"` record whenever a query exactly repeats an earlier query within the same `resolve_session()` session, tracked via a small per-session rolling cache (`state/search-session-cache.json`, last 50 queries). Purely observational — the cache itself is intentionally not built yet, pending this data. See `BACKLOG.md`'s Vector Search & Indexing section.
- **CL3 — Model-aware Claude hook thresholds** — Claude `truncate-output.py` and `compact-trigger.py` now scale their configured thresholds through `model_profiles.py`: Haiku runs tighter, standard Sonnet stays at the configured defaults, Opus and 1M Sonnet get more room. Tests pin the threshold selection and prove the Codex hook adapters do not enter the Claude model-profile path.
- **CL1 — Claude project budget caps** — `.less_tokens/config/budget.json` now populates `agent_overrides.claude` with tighter observe-mode caps for `retrieved_context`, `tool_output`, whole-file reads, single tool outputs, and broad directory listings, mirroring the Codex budget profile while leaving Codex's existing effective overrides unchanged. A regression test pins the Claude/Codex deep-merge behavior.
- **CX8 — Codex Bash context-cache** — Codex `context-cache.py` now wires Bash pre/post hooks, records short-TTL outputs for repeatable commands (`pwd`, `git status`, `rg`, and pytest), blocks duplicate Bash calls with an already-in-context nudge, and logs measured saved output chars.
- **CX7 — Patch-aware Codex post-edit diff caps** — Codex `apply_patch` post-edit context now starts with touched files plus compact hunk summaries, records each touched file for read-after-edit, and includes the full diff only when it fits under `CODEX_APPLY_PATCH_DIFF_CHARS`; Claude `Edit|Write` diff behavior is unchanged.
- **CX6 — Codex search result budget** — `search.py` now defaults to fewer Codex results (`k=2`) and shorter non-JSON snippets (400 chars) when `LESS_TOKENS_AGENT=codex`, while explicit `-k` / `--snippet-chars` values override the Codex defaults and Claude keeps the previous default behavior.
- **CX5 — Shrink Codex AGENTS.md fixed context** — compressed the installed Codex `AGENTS.md` fragment to rules plus a `less-tokens` skill pointer, while keeping concrete search, symbol, read-guard, AGENTS.md audit, navigation, and response-budget command details in the installed `less-tokens` skill.
- **CX11 — Codex hook event-contract CI** — added fixture-level tests that derive the installed Codex hook entries from `build_codex_hook_entries`, replay representative payloads for every `.codex/hooks.json` matcher, and cover filesystem reads/searches, Bash, `apply_patch`, `Edit`, and `Write` so Codex event-shape drift fails CI instead of silently degrading hooks.
- **CX10 — Codex enforcement parity audit** — added `codex_parity_audit.py`, a repeatable check that compares `.codex/hooks.json` against `agents/common/hooks/hook_manifest.py`, validates hook file writability, event/matcher/script coverage, and reports each strategy as `feature-parity` plus `best-effort-only` or `unwired`. The audit exits non-zero on missing/malformed/unwired Codex hook state and supports `--json` for CI or installer diagnostics.
- **CX4 — Codex Bash guard blocks broad high-token commands** — Codex `listing-guard.py` now blocks common noisy Bash shapes before they dump tokens: `find .` points to `rg --files`, broad `git diff` points to `--stat` / `--name-only`, verbose `pytest -v/-vv` points to `pytest -q` plus lean-output summaries, and plain `cat file` points to a bounded `sed` slice. Subprocess tests pin each Codex rewrite, and a shared listing-guard test proves Claude's common listing detector does not grow a `git diff` block.
- **CX3 — Codex truncate caps are tighter and configurable** — Codex `truncate-output.py` now uses Codex-scoped caps instead of Claude's `MAX_TOOL_OUTPUT_CHARS`: `CODEX_MAX_TOOL_OUTPUT_CHARS` defaults to `1500` and `CODEX_MAX_FILESYSTEM_READ_CHARS` defaults to `1200`, with `LESS_TOKENS_CODEX_MAX_TOOL_OUTPUT_CHARS` and `LESS_TOKENS_CODEX_MAX_FILESYSTEM_READ_CHARS` environment overrides. Subprocess tests pin the smaller caps for both `Bash` and `mcp__filesystem__read_file`; Claude truncation defaults stay unchanged.
- **CX2 — Codex installs wire the optional savings hooks by default** — `truncate-output`, `compact-trigger`, and `terse-reminder` are now included in a flagless `--agent codex` install, bringing the default Codex hook set to 17 entries. `--truncate --compact --caveman` remain accepted for back-compat and match the default set; `--no-truncate --no-compact --no-caveman` opt out.
- **CX1 — Codex budget overrides now take effect by default** — `agent_overrides.codex` ships with tighter nested `categories` and `hard_caps` for Codex (`retrieved_context`, `tool_output`, `diffs`, `full_file_read`, `single_tool_output`, and `directory_listing`) while `agent_overrides.claude` remains empty. `load_budget_config(..., agent="codex")` deep-merges those caps and preserves unspecified shared defaults; tests prove Claude's effective budget is unchanged and project-level Codex overrides do not wipe sibling Codex caps.
- **CL2 — Claude installs wire the optional savings hooks by default** — `truncate-output`, `compact-trigger`, and `terse-output`/caveman (the only `optional:true` rows in `parity.json`) are now wired on a fresh `--agent claude` install without needing `--truncate --compact --caveman`. New `--no-truncate`/`--no-compact`/`--no-caveman` flags opt out; the old positive flags are still accepted and match the default set. Pure `.claude/settings.json` generation change driven by `hook_manifest._optional_enabled`. The `--caveman` flag still owns the separate CLAUDE.md caveman-block append, which remains explicit.
- **Opt-in token-divisor calibration against Claude's real tokenizer** — Phase 6 of the stats framework rewrite (`stats_plan.md`). `stats.py --calibrate` samples representative repo content (root `*.md` prose + `.claude/tools/*.py` code, bounded for cost), counts it against Anthropic's `count_tokens` endpoint for the configured model (`--model`, default `claude-opus-4-8`), and computes the true chars-per-token for *our* content against the real model. It writes the divisor to `search_config.CHARS_PER_TOKEN` (with a dated comment) and a `state/calibration.json` sidecar; the report footer and HTML header badge then read `chars÷<n> calibrated <date>` instead of `chars÷4 uncalibrated`. With no tool-output capture store today, the basis is labeled `repo-sample`. Network + API-key gated, never automatic, and failure-safe: a missing SDK or auth error prints an actionable message and exits non-zero without touching state. Stored char counts are unchanged — only the report-time estimate is re-grounded. `stats.py` now reads `CHARS_PER_TOKEN` from `search_config`, and the `search.py`/`toolcost.py` cost displays became float-safe so a calibrated (non-integer) divisor renders cleanly.
- **Savings surfaced in the Claude interface** — Phase 5 of the stats framework rewrite (`stats_plan.md`). Two glanceable, measured-only surfaces read the same local log: (1) the `savings-html.py` Stop hook now prints a one-liner + `file://` link to `state/savings.html` (`↓ ~Nk tok saved (measured) · session — file://…`) so the page is one click away from the transcript, quiet until something is actually saved; (2) a Claude Code statusline (`stats.py --oneliner`) shows the same headline always-on. Both report measured strategies only (truncation, compaction) — upper-bound counterfactuals are never folded into the headline number, to stay honest. `install.py` wires the statusline merge-safely: it never clobbers a host project's existing `statusLine`, and uninstall removes only ours. Codex regenerates the same page after each tool call (`savings-html.py` on `PostToolUse`, since it has no native Stop hook, per `stats_plan.md` review #5).
- **Self-contained HTML savings page, regenerated every turn** — Phase 4 of the stats framework rewrite (`stats_plan.md`). `stats.py --html` writes `state/savings.html`, a single self-contained page (inline CSS only — no external links, scripts, fonts, or images) that opens straight from `file://`. It mirrors the Markdown report's honesty axis: measured and upper-bound savings render in separate panels with their own totals and are never cross-summed; session/all-time blocks, the chars÷4 token footer, and the always-on/local-only framing carry over. All rendered values are HTML-escaped. A `savings-html.py` hook regenerates the page after every turn so it stays current; it is failure-safe and silent (any error returns 0, never blocking the turn) and honors `LESS_TOKENS_NO_STATS=1`. Wired through the shared hook manifest (`savings-html`; Claude on `Stop`, Codex on `PostToolUse` — Codex has no native Stop equivalent per `stats_plan.md` review #5, so it refreshes after tools) and `parity.json` (now `shipped` for both agents).
- **Compaction savings are now measured** — Phase 2 of the stats framework rewrite (`stats_plan.md`). `compact-trigger.py` tracks the per-transcript peak size across calls; when Claude Code compacts the transcript (current size drops below half a peak that exceeded the compaction threshold), the shared `measure_compaction` helper logs a `basis="measured"` event with `kept_chars` = the post-compaction transcript (the produced summary) and `elided_chars` = peak − kept. Both sides are real char counts, so the compaction row is now an *actual* saving, not a placeholder. Fires only on a genuine shrink — a new/unknown transcript path just restarts tracking — and stays cheap and failure-safe. The report's methodology section and the `Compaction` row reflect the change.
- **Savings report explains its own methodology** — `stats.py --report` now emits a "How these numbers are measured" prose section in `savings-report.md`. It documents how each strategy derives `saved_chars` and grades how grounded each is: truncation is an actual byte cut, while search-first/search are counterfactual upper bounds (they credit the full size of files you would otherwise have read whole and never subtract the search cost paid). Also notes the chars÷4 token estimate (uncalibrated) and that "session" groups by the resolved `session_id`, falling back to an 8h wall-clock window only as a legacy view.

### Fixed
- **`savings-html.py` Stop hook silently no-opped** — it called `stats._stats_disabled()`, but that opt-out check lives in `savings_log`, not `stats`; the resulting `AttributeError` was swallowed by the hook's failure-safe `except`, so `state/savings.html` was never actually regenerated on Stop. Now imports `_stats_disabled` from `savings_log`. Caught while adding Phase 5 surfacing.
- **Codex install check false failures** — `install.py --check --agent codex` no longer requires `.claude/hooks/` for Codex-only installs, and `search_config.py` is loaded with `__file__` set so path-aware configs verify correctly.
- **install: `write_text(newline=...)` crashes on Python 3.9** — `write_python_launcher` wrote the launcher with `Path.write_text(..., newline="")`, but the `newline` kwarg was only added to `write_text` in Python 3.10. On 3.9 every install raised `TypeError: write_text() got an unexpected keyword argument 'newline'`. Now writes via `write_bytes(text.encode())`, preserving CRLF on all versions. Exposed by the integration suite once unit tests stopped failing first.
- **install: `UnicodeEncodeError` on Windows console** — status lines print non-ASCII glyphs (`→ ~ ✓ ·`); Windows stdout defaults to cp1252, which can't encode them, aborting the install mid-run. `main()` now reconfigures stdout/stderr to UTF-8 (guarded for non-TextIOWrapper streams). Surfaced by `install-e2e` Scenario A on windows-latest.
- **install: `.cmd` launcher rewritten on every run (idempotency bug)** — `write_python_launcher` compared the on-disk launcher via `read_text()`, which applies universal-newline translation (`\r\n`→`\n`), but the Windows `.cmd` launcher is written with CRLF and `newline=""`. The compare never matched, so the file was rewritten on every install and `changes` was never 0 — a clean re-run never reported "no changes". Now compares raw bytes (`read_bytes()` vs `text.encode()`); `read_text(newline=)` is unavailable before Python 3.13. Surfaced by `install-e2e` Scenario B once the workflow could actually run.
- **CI: `install-e2e` Scenarios A–C built the embedding index without deps** — install ran `embeddings.py refresh` (needs numpy/fastembed) even under `--skip-deps`, so every scenario failed with `ModuleNotFoundError: No module named 'numpy'`. Added `--no-build`; `index.db` is still created by the sqlite-only `db.py init` step, so the file-exists assertions still hold.
- **CI: `install-e2e` workflow never ran** — job-level `env` referenced `${{ runner.temp }}`, but the `runner` context is unavailable when job-level `env` is evaluated (no runner assigned yet), so every run failed at 0s with "workflow file issue" and zero jobs. Moved `HOST`/`HOST2` out of job `env`; they are now derived from `$RUNNER_TEMP` in a `Compute host paths` step that writes to `$GITHUB_ENV`.
- **CI: Windows path separator in `audit_rules()`** — `str(path.relative_to(BASE))` returned `\\`-separated paths on Windows; replaced with `.as_posix()`.
- **CI: Python 3.9 `SystemError` in `duplication()`** — CPython 3.9 bug triggers `SystemError: bad argument to internal function` when `from X import Y` is used inside a `try/except` inside a function; replaced with `import X as _x` style.
- **CI: `patch.object` fallback to `__builtins__` in duplication test** — fell back to patching `claudemd_audit.__builtins__` with a function when `embed` wasn't a module-level attribute; on Python 3.9, `__builtins__` is a dict so Python subscripted the function to look up `Exception`, causing `TypeError`; removed the `patch.object` wrapper.
- **CI: Windows codex launcher check** — `test_install_check` created `python` but `do_check` looks for `python.exe` on Windows; test now creates the platform-appropriate name.
- **CI: `test_recent_edit_blocks` Windows path mock** — test used Unix `/abs/foo.py` paths and patched `pathlib.Path.resolve` which doesn't work cross-platform; replaced with a real `tmp_path` file.
- **CI: subprocess strips `SYSTEMROOT` on Windows** — `env={"PYTHONPATH": ...}` replaced the full environment, removing `SYSTEMROOT`; Python 3.9 on Windows crashes at startup without it (`_Py_HashRandomization_Init` fatal error); fixed with `{**os.environ, "PYTHONPATH": ...}`.

### Added
- **Codex backlog completion (C1/C2/C3 + apply_patch extraction)** — hook wiring now comes from one shared manifest for Claude and Codex, with a CI-checked parity matrix in `agents/common/hooks/parity.json`. Codex `apply_patch` payloads now expose touched files to shared index-refresh logic, avoiding refreshes when patches only touch unindexed paths. Retired the already-shipped shared-hook extraction backlog item after confirming Claude and Codex wrappers use common context-cache, listing-guard, and post-edit-diff modules.
- **search.py cross-file semantic dedup (BACKLOG G10)** — `search()` already collapsed multiple chunks of one file to its best-scoring chunk; it now also drops a *distinct* file whose best chunk embeds near-identically (cosine ≥ `SEARCH_DEDUP_SIM`, default 0.97) to an already-selected hit, and backfills the freed slot with the next distinct hit — so two files with overlapping content aren't both paid for in one query. Pure post-processing on vectors already in hand; set `SEARCH_DEDUP_SIM >= 1.0` to disable. Covered by `test_search_semantic_dedup.py`.
- **`changelog_gate.py` merge gate (BACKLOG P2)** — turns the honor-system lifecycle rule into law with two checks: (1) a PR touching shipped `.py` under `.claude/`/`agents/` (tests excluded) must include `CHANGELOG.md` in its diff with a non-empty `## [Unreleased]` section; (2) when an entry ships a backlog item it cites the item's ID (`- [P2] ...`), and the gate fails if any cited ID still has a heading in `BACKLOG.md`, so a shipped item can't linger. Pure `check()` / `lingering_backlog_ids()` are unit-tested; wired into CI (`tests.yml`, PR-only) and `.pre-commit-config.yaml`.
- **`hunt_score.py`** — deterministic stop-rule scorer for bug hunts; reads `bughuntlog.jsonl`, evaluates three signals (severity slide, overlap rate, file coverage), prints `STOP` / `RUN ONE MORE` / `KEEP HUNTING` with per-signal breakdown.
- **`bughuntlog.jsonl`** — replaces `bughuntlog.md`; one JSON record per hunt round, schema documented in file header.

### Changed
- **Root-doc dedup to one canonical home each (BACKLOG F2)** — README, DOCUMENTATION.md, CONTRIBUTING.md, and `.claude/rules/caveman.md` carried overlapping copies of the same topics (fixed-bucket cost paid every session). Collapsed the non-canonical copies to one-line pointers: README's full repo-layout tree → pointer to DOCUMENTATION.md → Repository layout; DOCUMENTATION.md Contributing/License → pointers to CONTRIBUTING.md and `LICENSE`; DOCUMENTATION.md Caveman section → pointer to the canonical spec in `.claude/rules/caveman.md`. Added a "Root-doc canonical homes" table to the `claudemd` skill so future drift is caught during pruning.
- **CLAUDE.md prose pruned (BACKLOG P1/P3)** — deleted the "Search before Read" section (fully enforced by `search-first.py`, whose block message already prints the exact command) and the stale "Known bugs worth avoiding" note about `is_indexed()` divergence (both hooks already delegate to one shared `is_indexed` in `agents/common/hooks/search_first.py`; pinned by new `test_is_indexed_parity.py`). Lifecycle section shrunk to a pointer at the new gate. Cuts always-loaded per-turn tokens.
- **`caveman.md` collapsed to phrase list** — replaced conversational prose with a one-line rule + the canonical banned-phrase list from `VERBOSE_PATTERNS`, cutting always-loaded token cost ~50%.
- **`search_config.py` comment blocks trimmed to one-liners** — all multi-paragraph comment blocks reduced to single lines; file shrunk from 232 to 167 lines.
- **`evaluate.md` strategy sections compressed** — each S8–S13 section reduced from 100–200 words to 2 sentences (problem + enforcement); ~60% word reduction for tighter search hits.
- **Hook block messages trimmed** — removed trailing explanatory sentences from `search_first.py` and `grep_first_read.py` block messages; stripped "Options:" / comment suffixes / "To skip this gate" lines. Each gate block now emits filename + command only.
- **Skill descriptions tightened (BACKLOG F1)** — compressed the always-loaded `description` frontmatter of all five repo skills (`claudemd`, `agentsmd`, `lean-output`, `less-tokens`, codex `bug-hunt`): cut connective filler ("Use when asked to", "Moves … and fixes …") while preserving every trigger keyword and scope. Shrinks the fixed (per-turn) token bucket with no triggering change.

### Removed
- **`evaluate.md` retired** — every S8–S13 strategy it described has shipped, so the file was historical. Durable content found better homes: the lens (input/output/tool buckets), the "code over reasoning / a hook is law" principles, the shipped-strategy → enforcer table, and the deliberately-rejected ideas now live in `documentation.md` → *Token-reduction strategy* (indexed, searchable). The S6 verdict was already in `BACKLOG.md`; the hand-dated status line and one-time build order were dropped. `BACKLOG.md` references repointed; redundant `P4` folded into `C3`.
- **`strategy_21june26.md`** — stale strategy brainstorm doc; recommendation (token budgeter + relevance gate) shipped in full.
- **`strategy_21jun26_plan.md`** — completed implementation plan; all 7 phases shipped per results doc.
- **`strategy_21jun26_results.md`** — post-mortem for completed strategy work; acceptance criteria all green.
- **`multi-agent.md`** — completed multi-agent implementation plan; Codex adapter set shipped. `apply_patch` path extraction backlogged.
- **`codex-hook-coverage.md`** — point-in-time Codex hook coverage snapshot; content superseded by installed hooks. Three pending shared extractions backlogged.

### Fixed
- **Filler scan false positive on quoted examples** — `response_budget.py:analyze()` now strips single- and double-quoted spans before running the filler-phrase regex, so quoting a banned phrase as an example no longer triggers a caveman-mode violation.

### Added
- **Skill-description budget audit** — `claudemd_audit.py --skills` audits every `.claude/skills/*/SKILL.md` description (always-loaded so the model can decide when a skill applies) against `SKILL_DESC_WORD_CAP`, flagging over-cap descriptions, missing descriptions (skill won't trigger), and near-duplicate descriptions that compete to trigger (cosine ≥ `SKILL_DESC_DUP_SIM`, skipped gracefully without fastembed). Added `SKILL_DESC_WORD_CAP`, `SKILL_DESC_DUP_SIM`, and `SKILLS_DIR` config defaults; `--strict` exits 1 on over-cap or missing descriptions.
- **Rule-file token budget audit** — `claudemd_audit.py --rules` now audits markdown files under `.claude/rules/` with a per-file `RULES_TOKEN_BUDGET`, reporting token tax, stale refs, verbosity, and cut/trim verdicts just like the CLAUDE.md audit. Added `RULES_TOKEN_BUDGET` and `RULES_OVERFLOW_DOC` config defaults.
- **Budget-native context control plane** — added shared `.less_tokens/config/budget.json`, `agents/common/budget/`, Claude/Codex budget observer hooks, v2 telemetry in `.less_tokens/state/events.jsonl`, pressure-based compaction snapshots, and `.less_tokens/tools/budget_report.py` / `budget_doctor.py`. Modes are `observe`, `advise`, `enforce`, and `strict`, with explicit bypass support for rare broad-context needs.
- **`agentsmd` Codex skill** — added `agents/codex/skills/agentsmd/SKILL.md` so Codex has a first-class workflow for pruning always-loaded AGENTS.md content, running `agentsmd_audit.py`, moving searchable detail to indexed docs, and verifying recall. Codex installs now copy every skill under `agents/codex/skills/`, including `agentsmd`.
- **Expanded Codex parity hook set** — Codex installs now wire read-path and output-path protections comparable to Claude: `read-guard.py`, `auto-slice.py`, `grep-first-read.py`, `read-after-edit.py`, `context-cache.py`, `listing-guard.py`, `lean-output.py`, and `post-edit-diff.py` under `agents/codex/hooks/`. `build_codex_hook_entries()` now wires the full default Codex hook set, including truncation, compaction, and terse-output reminders. Added subprocess coverage in `test_codex_hooks.py` and install coverage in `test_install_codex.py`.
- **Shared Codex/Claude read-gate logic** — added agent-neutral modules in `agents/common/hooks/` for read guard, auto-slice, grep-first read, and read-after-edit. Codex adapters consume these shared checks; Claude hook paths remain stable.
- **Codex install health checks** — `install.py --check --agent codex` now verifies `.less_tokens/bin/python`, agent-aware `.less_tokens/tools/search_config.py`, `.codex/hooks/`, `.codex/hooks.json` entries, and the managed `AGENTS.md` block. The check path also accepts the current `documents` table schema as well as older `chunks` indexes.
- **JS/TS exact symbol lookup** — `symbols.py` now indexes JS/TS functions, classes, exported constants, interfaces, enums, and type aliases in addition to Python symbols. `/def` docs and the Codex `less-tokens` skill now describe Python and JS/TS coverage. Tests added in `test_symbols.py`.
- **Session-style evaluation harness** — new `.claude/tools/eval_sessions.py` provides a deterministic fixture-based comparison of broad baseline reads vs less_tokens-style targeted slices, with JSON and markdown report output. Tests added in `test_eval_sessions.py`.
- **Codex hook coverage documentation** — new `codex-hook-coverage.md` documents default and optional Codex hooks, shared logic, known limits, and verification commands.
- **`agentsmd-budget.py` Codex hook** — new `agents/codex/hooks/agentsmd-budget.py` mirrors `claudemd-budget.py` for AGENTS.md; fires after `Edit|Write`, exits 2 when over `AGENTS_MD_TOKEN_BUDGET` or stale refs found. Added `AGENTS_MD_TOKEN_BUDGET` (1200) and `AGENTS_MD_OVERFLOW_DOC` to `search_config.py`. Wired in `build_codex_hook_entries()`. 8 regression tests in `test_agentsmd_budget_hook.py`.
- **`claudemd-budget.py` wired as PostToolUse hook** — confirmed deployed via `.claude/hooks` tree and wired on `Edit|Write` in `build_claude_hook_entries()`; 6 regression tests added in `test_install_hook_entries.py` covering all conditional hooks (`--caveman`, `--compact`, `--truncate`).
- **`install.py --caveman` auto-appends caveman block to `CLAUDE.md`** — instead of printing a `cat` next-step, `--caveman` now writes an idempotent guarded block (`<!-- less_tokens:caveman:begin/end -->`) directly to `target_root/CLAUDE.md`; creates the file with a `# CLAUDE.md` header if absent. `--uninstall` removes the block. `_caveman_in_claude_md()` detects both new markers and legacy "Caveman Mode" text. 19 regression tests in `test_install_caveman_claude_md.py`.
- **`chunk_js` JS/TS chunker** — function-level chunking for `.js`, `.jsx`, `.ts`, `.tsx` files; matches named functions, classes, `const`/`let`/`var` assignments, TypeScript interfaces/enums/type aliases, and `export default`. Wired into `enumerate_sources()`. 19 regression tests added in `test_chunk_js.py`.
- **`install.py --check` regression tests** — 11 unit tests covering all check conditions: valid install returns 0, each failure path (`search_config.py` missing, `VENV_PY` unresolvable, `index.db` absent/empty, hooks dir absent/empty, `settings.json` absent/no hooks, smoke query failure) returns 1 with descriptive message. `--local` flag correctly targets `settings.local.json`.
- **`MAX_CHUNK_CHARS` in `search_config.py`** — configurable hard ceiling (default 2000 chars) on chunk body size; oversized chunks are split at paragraph/line boundaries into numbered sub-chunks (`key`, `key_2`, …). Set `0` to disable. Applied to all four chunkers (`chunk_markdown`, `chunk_python`, `chunk_sql`, `chunk_changelog`).

### Fixed
- **`terse-reminder` `stop_hook_active` guard covered for Codex** — confirmed the Codex terse reminder exits cleanly when `stop_hook_active` is set, preventing recursive reminder loops. Added subprocess coverage and removed the stale backlog item. `agents/codex/hooks/terse-reminder.py`, `test_codex_hooks.py`
- **`terse-reminder` now enforces single filler hits via the shared regex analyzer** — Codex terse enforcement uses `response_budget.analyze(..., min_filler_hits=1)`, matching Claude caveman filler detection instead of requiring multiple simple-string hits. Added subprocess coverage for a one-phrase response. `agents/codex/hooks/terse-reminder.py`, `test_codex_hooks.py`
- **`claudemd_audit.duplication()` now returns `{}` when no headed sections exist** — `embed([])` produced a shape-`(0,)` array causing `AxisError` in `np.linalg.norm(axis=1)`, caught by `except Exception → return None` and silently disabling dup detection; added `if not targets: return {}` guard. `claudemd_audit.py:196`
- **`claudemd_audit._strip_code` now strips unclosed code fences** — `re.sub(r"```.*?```")` left unclosed fences intact, leaking code into prose and inflating filler/word counts; changed to `r"```.*?(?:```|\Z)"` so an unclosed fence is stripped to end-of-string. `claudemd_audit.py:94`
- **`stats.py` `total_tokens` now computed from `total_chars`, not sum of per-row truncated values** — `sum(sc//4)` ≠ `sum(sc)//4`; strategies saving fewer than 4 chars each contributed 0 tokens to the total. `stats.py:100`
- **`mcp-prune._BASE` now resolves to host project root** — was always `Path(__file__).parent.parent.parent` (the less_tokens source tree); replaced with `_find_base()` that checks `cwd` for `.claude/tools/search_config.py` first, matching `db.py`'s approach. `mcp-prune.py:36`
- **`load_toolignore` now strips inline `#` comments** — `"slack  # note"` previously added verbatim to the ignored set, never matching the settings key `"slack"`; fixed with `line.split("#")[0].strip()`. `mcp-prune.py:46`
- **`connect_index()` return type annotation corrected to `_ClosingConn`** — was annotated `-> sqlite3.Connection` with a silenced `type: ignore`; callers using the return value directly (outside `with`) would get `AttributeError` since `_ClosingConn` has no `.execute` method. `db.py:55`
- **`caveman-reminder` no longer counts unclosed code fence content as prose** — `_FENCE` regex now matches from ` ``` ` to either the next ` ``` ` or end-of-string (`\Z`), so an unclosed fence's content is stripped before filler and word-budget checks. `caveman-reminder.py:67`
- **`symbols.py refresh()` now respects the `--full` flag** — `full=False` (default) performs an incremental refresh: only files modified since the marker are reprocessed and only their rows are replaced; `full=True` retains the previous full DELETE+reinsert behaviour. `symbols.py:127`
- **`post-edit-diff` now diffs against the file's own git repo** — `_diff_write` previously used `cwd=REPO` (the less_tokens directory), so writes to host-project files showed as all-new rather than diffs against the tracked version; fixed by using `cwd=p.resolve().parent`. `post-edit-diff.py:77`
- **`_locate_range` now finds the correct occurrence when first line is duplicated** — saves the fallback range but continues the loop; only returns the fallback after exhausting all candidates, so a later occurrence with a matching last line takes priority. `search.py:209`
- **`lean-ls` no longer crashes on broken symlinks** — classification now uses `is_dir()` / `is_file()` explicitly; broken symlinks (both return False) are silently skipped instead of being treated as directories and causing `FileNotFoundError` on `iterdir()`. `lean-ls.py:83`
- **`ensure_current_schema()` now calls `init()` when db file exists but has no schema** — checks `_current_version() == 0` after opening an existing file and routes to `init()` instead of `migrate()`, preventing `OperationalError: no such table: schema_version` when `connect_index()` created an empty file earlier in the session. `db.py:126`
- **`compact-trigger` no longer silenced by stale `last-size` from a prior session** — hysteresis guard now requires `last <= size` before suppressing; a new session whose transcript is smaller than the persisted value always fires when it exceeds the threshold. `compact-trigger.py:91`
- **`toolcost._recv()` now skips server notifications before returning the response** — `_read()` loops past JSON-RPC messages without an `id` field (server-sent notifications) instead of returning the first line unconditionally; prevents silent under-count of tools when a server emits a notification before the `tools/list` response. `toolcost.py:101`
- **`_resolve()` now uses rglob fallback for partial paths containing `/`** — removed `if "/" not in path` guard so `tools/search.py` correctly resolves to `.claude/tools/search.py` instead of returning None. `claudemd_audit.py:101`
- **`listing-guard` false-positive on `find . -type f` fixed** — added `type` to `_ALLOW_RE` so `-type f`/`-type d` are treated as selective predicates, not bare dumps. `listing-guard.py:99`
- **`listing-guard` tree detection changed from `re.match` to `re.search`** — chained commands like `echo foo && tree` or `cd /tmp; tree` now correctly trigger the guard; previously `re.match` only tested the start of the string. `listing-guard.py:81`
- **`duplication()` now filters by `embedding_model`** — query in `claudemd_audit.py` adds `AND embedding_model = ?` so stale-model rows with incompatible vector dimensions are excluded; prevents `np.vstack` `ValueError` that silently disabled duplicate detection after a model switch. `claudemd_audit.py:180`
- **`is_indexed()` in `index-refresh` now consults `INDEXED_ROOT_GLOBS` instead of hardcoded suffixes** — same fix as `search-first`: uses `fnmatch` against the imported glob tuple; added `INDEXED_ROOT_GLOBS` to the `search_config` import and fallback. `.claude/hooks/index-refresh.py:91`
- **`is_indexed()` in `search-first` now consults `INDEXED_ROOT_GLOBS` instead of hardcoded suffixes** — `_config["root_globs"]` is now used with `fnmatch` for root-file gating, keeping the hook in sync with the indexer. `.claude/hooks/search-first.py:81`
- **`offset=0` no longer treated as absent in `read-guard` and `read-after-edit`** — `inp.get("offset")` was falsy for `0`, causing `Read(offset=0, limit=N)` to be blocked as an unsliced whole-file read; changed to `is not None` check in both hooks. `.claude/hooks/read-guard.py:110`, `.claude/hooks/read-after-edit.py:68`
- **`truncate_bash` now uses assembled head+tail when `omitted_lines<=0`** — was calling `truncate_chars(text, ceiling)` on the original text (with trailing newline) unconditionally; now assembles `"\n".join(kept_head + kept_tail)` and only truncates if over ceiling, matching the normal branch. `.claude/hooks/truncate-output.py:77`
- **`_remove_gitignore_block` no longer leaves an orphan blank line after the managed block** — now also skips one blank line immediately following `_GI_END`, matching the existing behaviour for the preceding blank. `install.py:1090`
- **`grep-first-read` gate message now uses `VENV_PY` from `search_config`** — was hardcoding `.claude/.venv-tokens/bin/python`; users with a custom venv location now see the correct interpreter path in the gate hint. `.claude/hooks/grep-first-read.py:179`
- **`context-cache` no longer bleeds across sessions when `transcript_path` is `None`** — `_get_state(None)` now always returns a fresh empty state instead of matching the prior session's `""` key; stale "already read" entries from a previous sessionless run can no longer block reads in a new session. `.claude/hooks/context-cache.py:89`
- **Filename-only path match removed from `auto-slice` and `grep-first-read`** — `kp.name == p.name` falsely matched any two files sharing a basename regardless of directory; removed from both hooks, leaving only exact-path and suffix-path matching. `.claude/hooks/auto-slice.py:64`, `.claude/hooks/grep-first-read.py:98`
- **`savings_log` import failure no longer disables search-first gate** — separated `savings_log` import into its own try/except; a missing or broken `savings_log` now logs nothing but leaves the gate active. `.claude/hooks/search-first.py`
- **`_index_db_at_current_schema` now rejects stale schema versions** — was `bool(row[0])` (truthy for any version ≥ 1); now compares against `_INDEX_SCHEMA_VERSION` so a v1 index triggers migration instead of being silently accepted. `install.py:844`
- **`_extract_block` now captures full multi-line assignments** — added `end_lineno` parameter; `merge_search_config` passes `end_lineno` from AST so multi-line sets/dicts are injected complete instead of truncated to their opening line. `install.py:292`
- **Both agents now use venv-backed Python launchers instead of ambient `python3`** — installs generate `.claude/bin/python` and `.less_tokens/bin/python` (plus Windows `.cmd` siblings) that dispatch to the detected/created venv. Claude and Codex hook wiring, NEXT STEPS, docs, AGENTS.md, and Codex skill commands now use those launchers, so system Python can lack `numpy`/`fastembed` without breaking agent workflows. Added a shipped `.less_tokens/tools/read_guard.py` CLI for the Codex noise-file guard command and tests for launcher generation/wiring.
- **Codex parent-repo installs now run from installed paths** — `install.py --agent codex|both` now deploys shared hook support into `.less_tokens/hooks/`, patches `.less_tokens/tools/search_config.py` alongside the Claude config, and prints Codex-specific next steps that point at `.less_tokens/tools/`. Codex adapter hooks prefer installed `.less_tokens` paths (falling back to source-tree paths for tests), so installed hooks no longer crash with `ModuleNotFoundError: payload`. Added an `agentsmd_audit.py` wrapper so the Codex AGENTS.md/skill command exists after install, refreshed README/docs for the shared `.claude/index.db` + `.less_tokens` runtime layout, and made `--dry-run --create-venv` preview instead of aborting.
- **`embeddings.py refresh` touches `index.db` on no-op** — when a refresh finds nothing to re-embed, it now calls `os.utime` on `index.db` to advance its mtime; prevents `search.py` from emitting a spurious stale warning after git operations that update file mtimes without changing content.
- **`create_venv` bootstraps pip when missing** — after `python -m venv`, if `bin/pip` is absent (some macOS system Pythons skip it), `ensurepip --upgrade` is called automatically; `CLAUDE.md` test commands updated to use `.claude/.venv-tokens/bin/python -m pip/pytest` explicitly.

### Added
- **S12: Structured tool-output parsers (`lean-output`)** — new `.claude/tools/parse.py` with signal-only parsers for pytest (failing IDs + assertion lines + count), ruff/eslint (`file:line: code msg`), and git (name-status + stat). New `.claude/hooks/lean-output.py` PostToolUse hook on `Bash` auto-detects the tool in the command and pipes output through the parser, emitting only the signal via `hookSpecificOutput`. 60–95% size reduction on the noisiest repeated outputs. New `.claude/skills/lean-output/SKILL.md` for manual use.
- **Three-namespace architecture (`agents/`)** — shared hook logic extracted to `agents/common/hooks/` (`payload.py`, `search_first.py`, `index_refresh.py`, `truncate_output.py`, `compact_trigger.py`); Codex adapter hooks in `agents/codex/hooks/` (5 thin scripts: `index-refresh.py`, `search-first.py`, `truncate-output.py`, `compact-trigger.py`, `terse-reminder.py`); Codex install target `.less_tokens/` for agent-neutral shared core. `.codex/` receives wired hooks when writable. Three namespaces: `.claude/` (Claude), `.less_tokens/` (shared), `.codex/` (Codex).
- **Agent-aware state (`active_state_dir()`)** — `search_config.py` gains `CLAUDE_STATE_DIR`, `CODEX_STATE_DIR`, `state_dir_for()`, `active_state_dir()`, `_STATE_AGENT_AWARE` sentinel. All 8 Claude hooks and `search.py` updated to call `active_state_dir()` instead of the static `STATE_DIR`. Codex hooks write state to `.less_tokens/state/`; Claude hooks continue to write to `.claude/state/`. Backward-compat alias `STATE_DIR = CLAUDE_STATE_DIR` preserved.
- **`install.py --agent claude|codex|both`** — `selected_agents()` helper; `_install_specs()` now agent-aware (Codex deploys to `.less_tokens/tools/`, `.less_tokens/schema/`, `.codex/hooks/`); `build_claude_hook_entries()` (renamed from `_build_hook_entries`), `build_codex_hook_entries()`, `wire_codex_hooks_json()`, `unwire_codex_hooks_json()`, `handle_agents_md()` added; `_our_hook_names()` updated for Codex; `_GI_PATHS` includes `/.less_tokens/state/`; `do_uninstall()` agent-conditional. Default `--agent claude` keeps existing behavior.
- **Codex instructions + skill** — `agents/codex/instructions/AGENTS.md.fragment` (Token Discipline block managed via HTML comment sentinels); `agents/codex/skills/less-tokens/SKILL.md` (4-command workflow: search, symbols, read_guard, agentsmd_audit).
- **Multi-agent test coverage** — 8 new test files: `test_common_hooks.py` (payload normalizers, `is_indexed`, `search_was_recent`, `check_truncate_output`, `check_compact_trigger`), `test_search_config_agent.py` (`state_dir_for`, `active_state_dir`, env-var priority), `test_hooks_state_dir.py` (Claude/Codex state dir isolation via subprocess), `test_codex_hooks.py` (Codex adapter hook subprocess contract), `test_install_specs_agent.py` (`_install_specs` + `selected_agents`), `test_agents_md.py` (`handle_agents_md` idempotency), `test_install_codex.py` (codex install integration), `test_install_both.py` (both-mode install + selective uninstall).
- **Docs** — `documentation.md` gains `--agent` flag reference and Codex support section (compatibility matrix + known limitations); `README.md` updated with Codex badge and `--agent` quick-start examples.

- **G3: Directory & listing dump control (`lean-ls.py` + `listing-guard.py`)** — new `.claude/tools/lean-ls.py` replaces bare recursive listings with a depth-limited, `.gitignore`-aware, dir-count summary (`lean-ls [path] --depth N --max-per-dir M --max-total T`). New `listing-guard.py` PreToolUse hook on `Bash` intercepts `ls -R`, `find <path>` (without `-name`/`-iname`/`-path`/`-newer`/`-mtime` filters), and `tree` (without `-L` or with `-L > 3`), runs `lean-ls` against the target path, and exits 2 so Claude receives the compact listing instead of a raw dump. `truncate-output.py` extended with a Glob line-based cap: results exceeding `MAX_GLOB_RESULTS` (default 100) get a `"... N more files"` tail. Config: `LISTING_GUARD_ENABLED`, `MAX_GLOB_RESULTS` in `search_config.py`. Both hooks wired in `settings.json` and `install.py`.
- **G1: MCP schema token audit (`toolcost.py` + `mcp-prune.py` + `.toolignore`)** — makes the fixed per-turn MCP schema tax visible and controllable. `tools/toolcost.py` probes each configured MCP server via JSON-RPC over stdio, lists its tools, and reports estimated token cost per server in a sorted table with `★` markers on heavy servers (≥1,000 tokens) and `.toolignore` candidates. Falls back to a config-size estimate when a server can't be probed (SSE/HTTP servers, auth failures). Reads `mcpServers` from `~/.claude.json`, `.claude/settings.json`, and `.claude/settings.local.json` (merged, later wins). `tools/mcp-prune.py` reads `.claude/.toolignore` (one server name per line, `#` = comment) and removes matching entries from a settings file; supports `--dry-run` and `--in-place`. Tests: `test_toolcost.py` (27 tests).
- **G2: In-session re-read/re-search cache (`context-cache.py`)** — new PreToolUse hook on `Read|Grep` blocks repeat calls whose payload is already in context. Read entries are keyed on `(file_path, offset, limit)` + file mtime and are invalidated automatically when the file changes. Grep entries expire after `CONTEXT_CACHE_GREP_TTL` seconds (default 300). Each cached hit reports `"already in context (call #N, Xs ago)"` so Claude knows to skip the re-injection. Session boundary (detected via `transcript_path`) clears the cache on each new session. Config: `CONTEXT_CACHE_ENABLED`, `CONTEXT_CACHE_GREP_TTL` in `search_config.py`. Wired always-on in `settings.json` and `install.py`. Tests: `test_context_cache.py` (10 tests).
- - **Post-edit diff + re-Read block (S10)** — two new hooks eliminate the verify re-Read that follows every `Edit`/`Write`. `post-edit-diff.py` (PostToolUse on `Edit|Write`) computes a unified diff from `old_string`/`new_string` for edits or `git diff HEAD` for writes, emits it as `hookSpecificOutput` so Claude sees the change without re-reading, and records the path + timestamp to `STATE_DIR/last-edit.json`. `read-after-edit.py` (PreToolUse on `Read`) blocks a whole-file re-Read of the same file within `LAST_EDIT_WINDOW_SECONDS` (default 120 s); a `Read` with an explicit `offset` is always allowed. Diff capped at `MAX_DIFF_LINES` (default 60). Config: `LAST_EDIT_WINDOW_SECONDS`, `MAX_DIFF_LINES` in `search_config.py`. Tests: `test_post_edit_diff.py` (18 tests).
- **Grep-first Read gate (S13)** — new `grep-first-read.py` PreToolUse hook on `Read`: blocks whole-file reads of files exceeding `GREP_FIRST_LINE_THRESHOLD` lines (default 150) when no `offset` is given, routing Claude to `symbols.py <name>` or `search.py` to locate the target first, then read only the relevant slice. Exempts files already handled by `auto-slice.py` (in `last-search.json` + recent) and indexed files with no recent search (handled by `search-first.py`). Config: `GREP_FIRST_LINE_THRESHOLD` in `search_config.py`. Wired always-on alongside the other Read gates. Tests: `test_grep_first_read.py`.
- **Auto-slice Read (S9)** — `search.py` now records each hit's matched line range in `STATE_DIR/last-search.json`, and a new `auto-slice.py` PreToolUse hook turns a whole-file `Read` of a recent search hit into the exact `Read(offset, limit)` for that range (pass `offset` to override). Reads a 600-line file as a ~30-line slice. Only fires within `WINDOW_SECONDS` of the search. Tests: `test_auto_slice.py`.
- **Symbol index + `/def` (S8)** — new `tools/symbols.py` builds an AST-only `symbols` table (top-level defs/classes/UPPER_CASE constants → exact `file:line`), mirroring `chunk_python` selection. `symbols.py <name>` / the `/def` command print the location plus a ready `Read(offset,limit)` so a definition is read as a slice, not a whole file or a grep dump. `search-first.py` now also runs on `Grep`: when the pattern is a known symbol it adds a non-blocking hint to use `/def` (grep still allowed for usages). Works without fastembed. Tests: `test_symbols.py`.
- **Noise-file read guard (`read-guard.py`)** — new PreToolUse hook on `Read` that blocks an un-sliced read of high-noise files (lockfiles, `*.min.*`, source maps, notebooks, binaries detected by null bytes, and data files over `READ_DENY_DATA_MAX_LINES`) and tells Claude to slice or summarize instead. A `Read` with an explicit `offset` is always allowed. Complements `.claudeignore`. Config: `READ_DENY_GLOBS`, `READ_DENY_DATA_EXTS`, `READ_DENY_DATA_MAX_LINES`. Wired always-on alongside `search-first`. Tests: `test_read_guard.py`.

### Fixed
- **`embeddings._excluded()` NameError on `EXCLUDED_DIR_PREFIXES`** — `_excluded()` referenced `EXCLUDED_DIR_PREFIXES` but it was never imported from `search_config`, so any `embeddings.py refresh` (including the post-install build) crashed with `NameError` the moment it walked a source path; prefix-based exclusion was effectively dead code. Added the missing name to the `from search_config import (...)` block in `tools/embeddings.py`. Note: a prior "Bug 8" entry audited that the three exclusion sites applied the same logic but did not catch that this site's import was missing.
- **Caveman enforcement now inspects Claude's response, not tool output** — `caveman-reminder.py` was a PostToolUse hook scanning `tool_result` for filler, but filler lives in Claude's prose, so it almost never fired. Reimplemented as a `Stop` hook that reads the last assistant turn from `transcript_path` and exits 2 on filler phrases or prose exceeding `MAX_RESPONSE_WORDS` (code fences exempt); a `stop_hook_active` guard prevents loops. New config `CAVEMAN_ENFORCE` + `MAX_RESPONSE_WORDS`; installer wires it under the `Stop` event. Regression test: `test_caveman_reminder.py`.
- **Bug 30: `bug-hunt/SKILL.md` now references less_tokens project** — replaced stale AIPortfolio file coverage list and project path with less_tokens source files; severity rubric examples updated to match less_tokens failure modes.
- **Bug 29: `stats._load_records` emits WARN for malformed JSON lines** — confirmed by regression test: corrupt JSONL records print a warning to stderr; valid records are still returned.
- **Bug 28: `model_profiles.py` Opus entries are now consistent** — `claude-opus-3` raised to `recommended_k=8` (matching all other Opus models), and `claude-opus-4-8` added.
- **Bug 27: `_resolve_repo()` correctly handles CLAUDE.md-only projects** — confirmed by regression tests: the dev-env walk checks both `.git` and `CLAUDE.md`, and the `LESS_TOKENS_REPO` env var override takes precedence.
- **Bug 26: `stats._set_tracking` handles complex type hints** — regex broadened from `\w+` to `[^=]+` so `TRACK_SAVINGS: bool | None = ...` and `Optional[bool]` forms are matched correctly.
- **Bug 20: `search-first.py` no longer has a hardcoded 300-second fallback** — `search_was_recent()` now reads `_config["window_seconds"]` directly; the unreachable `.get(..., 300)` fallback that would have bypassed the configured value is removed.
- **Bug 19: `chunk_python` deduplicates source_key for same-named definitions** — when a file defines two functions or classes with identical names, each gets a unique `_N` suffix key so the UPSERT does not silently discard the first chunk.
- **Bug 18: `search()` now filters by `embedding_model`** — queries no longer score rows from a stale/switched model against the current query vector, eliminating garbage cosine scores in mixed-model indexes.
- **Bug 17: `truncate-output.py` does not log savings when disabled** — confirmed by regression test: with `MAX_TOOL_OUTPUT_CHARS=0`, `_log_savings` is never called regardless of result size.
- **Bug 10: `chunk_python` no longer emits trailing-space comment lines for blank docstring lines** — `_ctx()` now produces `#` (bare) instead of `# ` when the module docstring contains empty lines, eliminating hidden trailing whitespace in embedded chunk headers.
- **Bug 7: token warning uses `search_config.CHARS_PER_TOKEN` instead of hardcoded `4`** — chars-per-token ratio is now a named constant in `search_config.py` (default `4`) so users can tune it for code-heavy corpora without editing `search.py`.
- **`install.py --create-venv` uses `sys.executable` instead of hardcoded `"python3"`** — venv is now created with the exact interpreter that ran `install.py`, so version mismatches and missing-`python3`-on-PATH failures are eliminated.
- **Bugs 1 & 13: `chunk_changelog` now matches Keep a Changelog headers** — regex updated to `(?:\[.+?\]|v?\d+\.\d+\.\d+|\d{4}-\d{2}-\d{2})`, matching `## [Unreleased]`, `## [1.2.3] - 2024-01-01`, and bare date headers; confirmed fixed by test.
- **Bugs 2 & 11: `chunk_sql` uses a string-aware parser** — replaced naïve `;\n` split with a character-level scanner that tracks single/double-quoted strings and strips `--` comments, so semicolons inside string literals no longer split statements incorrectly; confirmed fixed by test.
- **Bugs 3 & 12: `chunk_python` handles tuple/list unpack assignments** — `MY_CONST, OTHER = 1, 2`, `(A, B) = (3, 4)`, and `[X, Y] = [5, 6]` are now indexed as individual UPPER_CASE names; confirmed fixed by test.
- **Bugs 4 & 5: `is_indexed()` is now consistent across hooks and embeddings** — `search-first.py`, `index-refresh.py`, and `embeddings._excluded()` all use the same `parts & EXCLUDED_DIR_NAMES` + `rel.startswith(prefix)` logic, and all three treat root `.py` and `.sql` files as indexed; confirmed fixed by code inspection.
- **Bugs 6 & 14: `_newest_source_mtime` now mirrors `enumerate_sources`** — includes root `*.py` and `*.sql` via `BASE.glob()` in addition to files under `INDEXED_SOURCE_DIRS`, matching the full set that `enumerate_sources()` would index; confirmed fixed by code inspection.
- **Bug 8: `EXCLUDED_DIR_PREFIXES` vs `EXCLUDED_DIR_NAMES` logic is consistent** — all three exclusion sites (`_excluded()`, `search-first.is_indexed()`, `index-refresh.is_indexed()`) apply both checks identically; confirmed fixed by code inspection.
- **Bug 9: `VENV_PY` default now points to `.claude/.venv-tokens`** — `search_config.py` default changed from `app/.venv` to `.claude/.venv-tokens`, matching the installer's actual venv location; confirmed fixed by code inspection.
- **Bug 15: `_ClosingConn` missing methods are non-functional** — every call site uses `with connect_index() as c:`, which returns the unwrapped `sqlite3.Connection` via `__enter__`; the wrapper's limited surface is never accessed directly; confirmed non-issue by code inspection.

### Changed
- **All source directories moved into `.claude/`** — `tools/`, `schema/`, `tests/`, `hooks/`, and `caveman/` now live under `.claude/` in the source repo (`less_tokens/.claude/`). Nothing in the deployed layout changes — artifacts were already installed into `.claude/`. The repo tree now matches exactly what gets deployed, and the bughunt protocol is a native Claude Code skill at `.claude/skills/bug-hunt/SKILL.md`. The caveman output style is a Claude Code rule at `.claude/rules/caveman.md`.
- **Installer deploys all artifacts into `.claude/`** — `tools/` → `.claude/tools/`, `schema/` → `.claude/schema/`, `index.db` → `.claude/index.db`, and the venv → `.claude/.venv-tokens/`. Keeps AI-tooling artifacts out of the host project's root directories, making them invisible to linters, import scanners, and source-tree tooling. Each tool script uses a three-level path anchor (`Path(__file__).parent.parent.parent`) to reach the project root from its deployed location under `.claude/tools/`. `INDEXED_SOURCE_DIRS` now defaults to `()` — the installer auto-discovers host Python source dirs, since `tools/` and `schema/` no longer live at the root and are not sensible defaults for host projects

### Added
- **`install.py --update`** — safe upgrade path: re-copies `tools/`, `schema/`, and `.claude/hooks/` (implies `--force-hooks --force-tools --overwrite-modified`) but never touches `tools/search_config.py` or `index.db`. Skips the DB init/migrate step and the `VENV_PY` / `INDEXED_SOURCE_DIRS` auto-patches. Rejects `--force-config` / `--force` / `--build` so the safety contract can't be silently violated
- **Installer auto-patches `INDEXED_SOURCE_DIRS`** — after copying `search_config.py`, the installer scans the host repo for top-level directories containing `.py` files (skipping `.venv*`, `__pycache__`, `node_modules`, etc.) and rewrites the default `("tools/", "schema/")` to that discovered set. Same conservative posture as `patch_venv_py`: only fires when the existing value is still the source default, so user customizations are preserved
- **Recursive-glob guidance for doc-heavy repos** — `search_config.INDEXED_ROOT_GLOBS` now carries an inline comment documenting `pathlib`'s `**` support (e.g. `"docs/**/*.md"`, `"**/*.md"`), `install.py` NEXT STEPS calls out the `INDEXED_ROOT_GLOBS` (`.md`) vs `INDEXED_SOURCE_DIRS` (`.py`/`.sql`) split, and a heuristic tip prints when the target has no root-level `.py` files but ≥5 markdown files in subdirectories (suggests switching to `("**/*.md",)`)
- **`AGENT_MODEL` config + `tools/model_profiles.py`** — declare the Claude model this install targets (e.g. `AGENT_MODEL = "claude-sonnet-4-6"`) and `search.py` picks up the model's recommended default `k` from the new lookup table (`tools/model_profiles.py`, keyed by Claude model id → context_window + recommended_k). Explicit `-k` still wins; unset model = current behavior (DEFAULT_K = 3). `search.py` also prints a stderr warning when returned chunks risk consuming a large fraction of the configured model's window
- **`install.py --local`** — wire hooks into `.claude/settings.local.json` (personal / untracked) instead of the project-shared `.claude/settings.json`. Trade-off documented in the help: Claude Code's permission rewrites can clobber the hooks block in `settings.local.json`, which is why the default stays on the shared file
- **`install.py --create-venv`** — when no venv is detected, create `.venv-tokens` and continue in a single pass instead of aborting with a "create then re-run" instruction. Refuses to clobber a pre-existing `.venv-tokens` so a partial venv is never silently overwritten
- **`embeddings.py switch-model`** — atomically rewrites `EMBEDDING_MODEL` + `EMBEDDING_DIM` in `search_config.py` and runs `refresh --full`, preventing the silent dimension mismatch that occurs when the model is changed by hand but the dim isn't (scores quietly become wrong against the existing index). Refuses a no-op (same model + same dim) so a misclick doesn't trigger a gratuitous full re-index. Usage: `python3 tools/embeddings.py switch-model BAAI/bge-base-en-v1.5 --dim 768`
- **`.pre-commit-config.yaml`** — opt-in pre-commit wiring for `ruff` (lint + format). Contributors run `pip install pre-commit && pre-commit install` once and get the same lint feedback locally that CI enforces. Pyright is omitted for now (not part of the test toolchain)
- **Optional module-docstring context in code chunks** — new `CHUNK_INCLUDE_MODULE_CONTEXT` flag in `search_config.py` (default `False`); when enabled, `chunk_python` prefixes each top-level def/class chunk with the file's module docstring (as a comment) so a single search hit conveys the file's purpose without a follow-up Read. Opt-in because it changes content hashes and re-embeds all Python sources on the next refresh
- **`embeddings.py stats --verbose`** — extends `stats` with a dashboard: indexed-file count, index age (derived from the newest chunk's `updated_at`), and coverage vs expected sources (`covered/expected` + percent, with up to 10 missing paths listed). Plain `stats` output is unchanged
- **`search.py` query history log** — every run appends one best-effort JSONL record (UTC timestamp, query, top result score, result count) to `.claude/state/search-history.log`, so maintainers can audit what Claude searched for and spot queries that consistently return weak results. Logging failures are swallowed — the audit log never breaks the search it records
- **`embeddings.py refresh --dry-run`** — preview a refresh without writing: prints `add` / `update` / `unchanged` / `delete` chunk counts (and treats every existing row as a delete under `--full`). Loads no embedding model and leaves `index.db` untouched, so it works even when fastembed isn't installed
- **Stale-index warning in `search.py`** — before results, `search.py` compares the newest indexed source-file mtime against `index.db` and prints a one-line `WARN: index may be stale … run tools/embeddings.py refresh` to stderr when a source is newer (the auto-refresh hook can lag or fail). Stderr only — stdout/JSON output is unchanged
- **`search.py --min-score`** — drop top-k results whose cosine score is below a floor (e.g. `--min-score 0.5`), so Claude doesn't act on semantically unrelated chunks that merely rank in the top-k. `search()` also takes a `min_score` kwarg; unset (default) keeps current behavior
- **Install E2E workflow** (`.github/workflows/install-e2e.yml`) — subprocess-level test of `install.py` across Ubuntu / macOS / Windows. Covers fresh install (cwd-independent target resolution), idempotent re-run from inside the clone, `--target` override, suspicious-target sanity-check abort, and source-self guard. Complements the function-level coverage in `tests/integration/test_install.py`, which the existing Tests workflow runs
- **`patch_venv_py` tests** in `tests/integration/test_install.py` — six cases covering the default-value patch, idempotent re-run, user-customization preservation, missing-`VENV_PY` no-op, absolute-path fallback when the venv lives outside `target_root`, and preservation of surrounding lines/comments
- **`install.py --dry-run`** — preview the full blast radius before mutating the host project: resolved target, every file that would be copied (new/skip/overwrite), venv detection result, `search_config.py` variables that would be injected, and the `.claude/settings.json` hooks that would be wired — all without writing anything
- **`install.py --uninstall`** (with `--purge-index`) — reverse a deployment: strip the less_tokens hook entries from `.claude/settings.json`, remove the copied `tools/`, `schema/`, and `.claude/hooks/` files, and with `--purge-index` also drop `index.db` and its WAL sidecars — while leaving host-authored files and `tools/search_config.py` untouched
- **`install.py --gitignore`** — add a managed `.gitignore` block for generated artifacts (`index.db`, `.claude/state/`) so they don't pollute the host repo; a one-time tip points to the flag when the block is absent
- **Namespace-collision guard (`--allow-merge`)** — the installer now detects pre-existing non-less_tokens files in the host's `tools/` or `schema/` and aborts before any write; `--allow-merge` opts into merging anyway
- **`INDEXED_DOC_GLOBS` config var** — index markdown that lives outside the repo root (e.g. `docs/*.md`, or a vendored subproject's docs) alongside `INDEXED_ROOT_GLOBS`. Defaults to empty, so existing installs are unaffected; entries are keyed by full path relative to the repo so same-named files at different depths stay distinct

### Changed
- **`install.py` builds the index by default** — a default install now runs `embeddings.py refresh` (and the post-build smoke check) so a single command leaves the project ready to search. Adds ~30s and a ~130 MB model download on first run; `--no-build` opts out and prints the manual build commands in NEXT STEPS (replaces the old opt-in `--build`)
- **Installer warns when modifying a committed `.claude/settings.json`** — when the default-target file exists and is non-empty, a one-line notice prints before write so a shared / change-controlled file is never silently edited (the `--local` opt-out is also mentioned)
- **Managed `.gitignore` block is now default-on** — `install.py` adds the `index.db` / `.claude/state/` ignore block by default in any git target; opt out with `--no-gitignore` (replaces the old opt-in `--gitignore`). Removes the silent `git status` pollution after a fresh install
- **`install.py --build` runs a post-build smoke check** — after a successful `embeddings.py refresh`, the installer now invokes `embeddings.py stats` and exits non-zero if it fails, so a broken or empty index is caught at install time rather than on the user's first search. `stats` (not `health`) is used to avoid false alarms on host repos whose `INDEXED_SOURCE_DIRS` haven't been customized yet
- **Installer NEXT STEPS announces search-first activation** — `install.py` now prints a final note explaining that the PreToolUse hook is live for any already-running Claude session in the target, will block Reads on indexed files until a search runs, and points at `WINDOW_SECONDS` in `search_config.py` for tuning. Removes the "why is Read suddenly erroring?" debugging trip after a fresh install
- **`WINDOW_SECONDS` is now configurable** — the search-first hook's gate window (default 300s) lives in `search_config.py` instead of being hardcoded in `hooks/search-first.py`. Tune the aggressiveness of the search-before-Read gate without editing hook source
- **Venv detection prefers `$VIRTUAL_ENV`** — `install.detect_venv()` and `search_config._venv_python()` now check the `VIRTUAL_ENV` env var first (set by `activate`, reliable on all platforms) before the relative-path guesses. An activated venv is auto-used without editing `search_config.py` or passing `--venv`; falls back to the previous behavior when unset or stale
- **`search.py` deduplicates results by file** — when multiple top-k chunks come from the same `source_path`, only the best-scoring chunk is kept and the freed slot is filled with the next distinct file, so `k` results cover `k` files instead of near-duplicate chunks of one. `--min-score` still applies
- **Embedding model + dimension are now configurable** — `EMBEDDING_MODEL` / `EMBEDDING_DIM` live in `search_config.py` instead of being hardcoded in `embeddings.py`, so users can switch embedding models without editing tool source. `embeddings.py` re-exports them as `MODEL` / `DIM` and `search.py` reads `DIM` via that re-export, so the value is config-sourced end to end (change both together and re-run `embeddings.py refresh --full`)
- **Installer auto-patches `VENV_PY`** — after detecting the venv, `install.py` rewrites `VENV_PY = _venv_python("...")` in `tools/search_config.py` to the detected venv (relative to the host project when possible). Conservative match: only fires when the existing value is still the source default, so user customizations are preserved. NEXT STEPS output now skips the manual VENV_PY instruction when the auto-patch lands, leaving only `INDEXED_SOURCE_DIRS` as project-specific configuration the user must edit
- **Installer targets the parent of the clone instead of `cwd`** — `install.py` now derives its target from `Path(__file__).resolve().parent.parent`, so cloning less_tokens into a host project (`cd ~/myproject && git clone ... less_tokens`) and running `python3 less_tokens/install.py` from anywhere always installs into `~/myproject`. This matches the documented "clone in, install up" workflow and makes `git pull && python3 install.py` from inside the clone a working upgrade path
- New `--target PATH` flag overrides the auto-derived target (useful for scratch projects, CI, testing); `--yes` bypasses the new suspicious-target sanity check that aborts if the auto-derived parent resolves to `/` or `$HOME` (catches a less_tokens clone that wasn't placed inside a project)
- **Benign urllib3 LibreSSL warning is suppressed** — on macOS system Python (LibreSSL), importing `fastembed` made every `search.py` run print a `urllib3 NotOpenSSLWarning` to stderr before results, polluting hook output and any captured search results. `embeddings.py` now installs a process-wide filter for that known message at import; `search.py` imports `embeddings` at module load, so both entrypoints are quiet

### Fixed
- **`search-first.py` docstring now names the correct settings file** — it said `install.py` wires the hook into `.claude/settings.local.json`, but the installer deliberately writes the project-shared `.claude/settings.json` (so Claude rewrites can't clobber the hooks block). The stale docstring misled anyone debugging why the gate wasn't firing
- **The search-first gate no longer blocks Reads of `.md` files it can never clear** — `is_indexed()` (in both `hooks/search-first.py` and `hooks/index-refresh.py`) treated any `.md` under an `INDEXED_SOURCE_DIRS` entry as indexed, but `enumerate_sources()` only collects `*.py` / `*.sql` from those dirs — never `*.md`. Adding a directory to `INDEXED_SOURCE_DIRS` therefore made the gate block Reads of `.md` files absent from `index.db`, with no scoped search able to clear it. The `INDEXED_SOURCE_DIRS` branch now matches only `.py` / `.sql`, so the gate reflects what is actually indexed (root and doc-glob markdown are unaffected)
- **Repeated markdown headings no longer silently drop a chunk when a literal `## Foo_2` also exists** — `chunk_markdown` deduped a repeated `## Foo` to source key `Foo_2`, which collided with a literal `## Foo_2` heading in the same file; the two chunks shared `(source_path, source_key)` and the index UPSERT silently kept only one, so a whole section became unsearchable. Dedup suffixes are now chosen to skip any key that already exists literally in the file (or was already emitted), so every chunk keeps a unique key
- **A single permission-denied directory no longer aborts the whole index refresh** — `embeddings.py refresh` walked each `INDEXED_SOURCE_DIRS` entry with `rglob`, which raised `PermissionError` and killed the entire run (leaving `index.db` stale) the moment it hit one unreadable subtree, even when every other source was readable. Unreadable directories are now skipped with a `WARN` and the remaining sources are still indexed
- **A single permission-denied directory no longer crashes `embeddings.py health` / `db.py verify`** — `expected_source_paths()`, the coverage check behind `health` and `verify`, had the same `rglob` flaw as the refresh path: one unreadable subtree raised `PermissionError` and crashed the whole report instead of listing coverage, even when every other source was readable. Unreadable directories are now skipped with a `WARN` and the remaining sources are still reported
- **A transiently-unreadable source directory no longer silently deletes its indexed rows** — once `refresh` learned to skip an unreadable subtree and continue, it received a *partial* source list, and its prune step deleted every index row not present in that partial list (`refresh --full` wiped the table outright), so a temporarily-locked directory lost its still-usable search entries until a full rebuild. `refresh` now detects an incomplete enumeration, skips both delete paths with a `WARN`, and keeps the existing rows until a clean refresh reconciles them
- **Embedding vectors are stored little-endian so `index.db` is portable across CPU endianness** — the writer serialized vectors with the host's native byte order and the reader decoded them the same way, so an `index.db` built on a little-endian machine and read on a big-endian one (s390x, some POWER/ARM) returned silently wrong cosine similarity scores with no error. Serialization is now pinned to little-endian `<f4` at a single shared point used by both the indexer and the search reader
- **Pre-fix indexes are auto-invalidated on upgrade so old vectors aren't misread** — bumping the little-endian fix above without re-indexing would leave content-hash-unchanged rows in their old native byte order while the reader now always decodes `<f4` (a v1 index built on a big-endian host would score silently wrong). The `index.db` schema is now **v2**: `db.py migrate` drops the `documents` rows when crossing the v1→v2 boundary, and `embeddings.py refresh` calls a new `ensure_current_schema()` at startup so this one-time invalidation runs automatically on the normal upgrade path (`install --build`, a manual `refresh`, or the index-refresh hook) — no manual `db.py migrate` needed. Note: byte order was not recorded in v1, so the first refresh after upgrading performs a one-time full re-embed for **all** users (≈30 s plus model download if not cached), not only big-endian ones
- **An `index.db` built before the little-endian pin is auto-rebuilt instead of returning silently corrupt scores** — pinning vectors little-endian did not fix indexes already written on a big-endian host: `refresh` only re-embeds a row when its source *text* changes, so the old native-endian blobs survived and `search` decoded them little-endian — wrong cosine scores with no error until a manual full rebuild. `refresh` now records a vector-layout marker in the database (`PRAGMA user_version`); an index that predates the marker is fully re-embedded once and then stamped, so the next routine refresh self-heals it. The forced rebuild is deferred when a source directory is unreadable, so it never deletes rows it cannot re-embed
- **`search.py --source-type` choices now track the index instead of a static list** — `--source-type` was validated against a hardcoded `SOURCE_TYPES` that had drifted from `documents.source_type`: it advertised values the indexer never produces (`journal`, `note` → always zero results) and rejected values a different or older index legitimately contained. Choices are now derived from `SELECT DISTINCT source_type FROM documents` at runtime, and left unconstrained when the index is unavailable
- **Venv paths containing a quote or backslash no longer write an invalid `search_config.py`** — `install.py` interpolated the detected venv path straight into the `VENV_PY = _venv_python("…")` line it writes (and the next-steps hint it prints), so a path with an embedded `"` produced a `SyntaxError` in the host config (and an un-pasteable hint). Both sites now emit a JSON-escaped string literal via a shared `_venv_python_call()` helper — byte-identical to the previous form for ordinary paths
- **`index-refresh.py` now detaches the background refresh on Windows** — `subprocess.Popen(..., start_new_session=True)` is POSIX-only and was silently ignored on Windows, leaving the `embeddings.py refresh` child attached to the Claude Code process; the hook now branches on `sys.platform`, passing `creationflags=DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP` on Windows and keeping `start_new_session=True` on POSIX
- `tests/integration/test_install.py` updated for the new `target_root` parameter on `copy_tree` / `handle_search_config`; tests no longer monkeypatch a now-removed module-level `TARGET_ROOT`
- **Installer no longer leaves a silent half-install on abort** — the venv is resolved/validated and the namespace-collision check now runs *before* any files are copied, so a missing venv or other failed precondition aborts with nothing written, instead of copying hooks that were never registered with Claude Code and leaving the toolkit silently inert
- **Markdown indexed by relative path, not bare filename** — `enumerate_sources` keyed root markdown by `f.name`, so two same-named `.md` files reachable from the index (e.g. a root `CLAUDE.md` and one under a vendored subdir) collided on `(source_path, source_key)` and the UPSERT silently dropped one. Markdown is now keyed by path relative to the repo root; root entries are unchanged (`CLAUDE.md` stays `CLAUDE.md`)

### Added
- **Token savings tracking** — new `tools/savings_log.py` + `tools/stats.py`; disabled by default (`TRACK_SAVINGS = False` in `search_config.py`); enable with `python tools/stats.py --enable` (or the interactive prompt); hooks log chars saved per strategy to `.claude/state/savings.jsonl`; `--report` writes a markdown summary to `.claude/state/savings-report.md`; `--all` shows all-time totals; `--disable` turns tracking off; `embeddings.py savings` dispatches to the same interface
- `TRACK_SAVINGS` config variable in `tools/search_config.py`
- **Stats GitHub Actions workflow** (`.github/workflows/stats.yml`) — runs stats unit tests on Python 3.9 / 3.11 / 3.12 × all three OS, plus a separate job that enables tracking, logs synthetic events, generates a savings report, uploads it as a workflow artifact, and resets tracking to off
- Unit tests for `savings_log` and `stats` in `tests/unit/test_stats.py` (append guard, timestamp injection, record filtering, summarize aggregation, table rendering, report generation, config patching)

### Fixed
- `is_indexed()` in `hooks/index-refresh.py` now uses the same mid-path exclusion check as `search-first.py` (`("/" + d) in ("/" + rel)`), so both hooks agree on files inside excluded directories that appear at non-root path positions
- `chunk_sql` no longer splits SQL statements on semicolons that appear inside `--` line comments; comments are stripped before splitting so statement boundaries are found correctly
- Removed `"app/"` from the default `INDEXED_SOURCE_DIRS` in `tools/search_config.py` — the installer never creates that directory, so fresh installs no longer report a health gap for every file under `app/`
- `chunk_changelog` now recognises Keep-a-Changelog headers (`## [version] - date`, `## [Unreleased]`) in addition to date-only headers (`## YYYY-MM-DD`); previously any CHANGELOG using the standard format fell back to `chunk_markdown` and lost per-version chunk structure

### Added
- **`.claudeignore`** — ships with the toolkit to exclude documentation (`README.md`, `documentation.md`, `CHANGELOG.md`), CI config (`.github/`), bug-hunt files (`bughunt/`), benchmark artifacts (`tests/perf/latest.json`), and target-project templates (`caveman/caveman.md`) from Claude's project file scope; reduces noise in file suggestions and directory listings during code work
- **Test suite and CI** — `tests/unit/` covers chunkers, db, hook protocol, `is_indexed` parity, config merge, and settings wiring; `tests/integration/test_install.py` runs end-to-end installer scenarios; `tests/perf/test_bench_tokens.py` benchmarks token-reduction per strategy; GitHub Actions matrix runs unit and integration tests on Python 3.9 / 3.11 / 3.12 × Ubuntu / macOS / Windows, perf benchmarks on Ubuntu with fastembed model caching and artifact upload
- **Non-destructive installer** — `install.py` now handles re-runs and upgrades safely:
  - `search_config.py` is never overwritten wholesale; only variables absent in the existing file are injected, preserving all user-set values
  - `copy_tree` detects locally-modified files and shows a `+N -M lines` diff summary instead of silently overwriting; `--overwrite-modified` is required to update them
  - `.claude/settings.local.json` hook wiring is now automatic and idempotent — the installer merges entries by `(event_type, matcher, command)` identity so re-running never duplicates hook entries
  - `--caveman` next-steps hint checks whether the caveman section heading already exists in `CLAUDE.md` and prints "already present" instead of repeating the append instruction
  - Granular force flags: `--force-hooks`, `--force-tools`, `--force-config` (each scoped to one directory); `--force` remains a shorthand for all three
- `tools/db.py init` now records schema version in the `schema_version` table on first run
- `tools/db.py migrate` subcommand applies pending schema migrations and updates the recorded version

### Added
- **Tool output truncation (Strategy 3)** — new `hooks/truncate-output.py` PostToolUse hook caps oversized Bash, Read, and WebFetch results to a configurable character ceiling (default 4000 ≈ 1000 tokens), saving 40–80% of tool-output tokens on verbose commands like `git log`, test runners, and large file reads
- Bash output uses head+tail truncation (preserves first 50 + last 20 lines so command start and trailing errors both survive); Read/WebFetch use a 60/40 character split
- Three new config variables in `tools/search_config.py`: `MAX_TOOL_OUTPUT_CHARS`, `TOOL_OUTPUT_HEAD_LINES`, `TOOL_OUTPUT_TAIL_LINES` (set `MAX_TOOL_OUTPUT_CHARS = 0` to disable without unwiring the hook)
- `--truncate` flag to `install.py` to print the truncation hook's `settings.local.json` wiring in the next-steps output
- **Conversation compaction trigger (Strategy 5)** — new `hooks/compact-trigger.py` PostToolUse hook reads the session transcript path from the hook payload, gates on `MAX_SESSION_CHARS` (default 500 KB ≈ 125k tokens), and nudges Claude to run `/compact` when the threshold is crossed; saves 50–70% of input tokens on long sessions
- Hysteresis state at `.claude/state/compact-trigger-last` ensures the reminder only re-fires after the transcript grows another 25%, so it won't spam every subsequent tool call once tripped
- New config variable `MAX_SESSION_CHARS` in `tools/search_config.py` (set to 0 to disable)
- `--compact` flag to `install.py` to print the compaction trigger's `settings.local.json` wiring in the next-steps output
- **Cline adapter (`adapters/cline/`)** — first non-Claude-Code agent integration. Ships a FastMCP stdio server (`mcp-search/server.py`) that exposes the project's vector search as an MCP `search` tool Cline can call natively, plus `.clinerules/` instruction files (`01-search-before-read.md`, `02-caveman.md`) that port Strategies 1 and 2 to Cline's project-rules format
- `adapters/cline/install-cline.py` — adapter installer: copies `.clinerules/`, patches `STATE_DIR` to `.less_tokens/state/`, installs the `mcp` SDK into the project venv, prints the OS-specific `cline_mcp_settings.json` snippet for user-level MCP registration
- New `STATE_DIR` config variable in `tools/search_config.py` (default `.claude/state/`) so non-Claude-Code adapters can override it without leaving a stray `.claude/` directory in unrelated projects
- `.gitignore` now ignores `.venv/`, `venv/`, `env/`, `app/.venv/` so contributors can't accidentally commit a virtual environment

### Changed
- `tools/search.py` and `hooks/search-first.py` now read the search-first state file path from `STATE_DIR` in `search_config.py` instead of hardcoding `.claude/state/`
- `tools/db.py` and `tools/search.py` now pass `encoding="utf-8"` explicitly on text IO so non-ASCII content survives Windows hosts (default cp1252)
- `hooks/caveman-reminder.py` verbosity patterns also catch `Certainly.`, `Absolutely.`, and `Of course.` (period endings, the most common shape) — previously only matched `,` and `!`
- `tools/embeddings.py` uses `datetime.now(timezone.utc)` instead of the deprecated `datetime.utcnow()` (Python 3.12+ DeprecationWarning)
- `hooks/index-refresh.py` drops the redundant `VENV_PY as _VENV_PY` import alias

### Fixed
- `tools/search.py` now catches `sqlite3.OperationalError` and returns an empty result with a stderr advisory when `index.db` is missing or uninitialised (previously crashed)
- `hooks/search-first.py` `search_was_recent()` no longer races between `exists()` and `stat()`; `FileNotFoundError` is handled instead of crashing the gate
- `README.md` quickstart removed the `--skip-build` flag that was deleted from `install.py` in 0.2.0 (the default behaviour is already to skip the build; `--build` is the opt-in)
- `hooks/search-first.py` docstring example replaced `python3` with the venv-python placeholder so Windows users aren't pointed at a non-existent command
- `schema/index.sql` `embedding` column comment corrected from `float32[1024], voyage-3-lite output` to reflect the actual model and 384-dim output
- `tools/search.py` removed a stale comment referencing a non-existent `voyage_embed` function

## [0.2.0] - 2026-05-03

### Added
- **Caveman mode** — new `caveman/caveman.md` CLAUDE.md snippet that instructs Claude to respond in terse, primitive prose, reducing output tokens 30–60%
- `hooks/caveman-reminder.py` — PostToolUse hook that detects verbose filler patterns ("Certainly!", "I apologize", "I'd be happy to", etc.) and nudges Claude back to caveman style
- `--caveman` flag to `install.py` to copy caveman assets during install
- `--build` flag to `install.py` to opt into building the index immediately after install
- Prerequisites section in README (Python 3.9+, venv, Claude Code)
- Badges in README (Python version, platform, Claude Code)
- Table of contents in README
- Contributing and License sections in README

### Changed
- `--skip-build` is now the default in `install.py` — prevents a wasted index build before `search_config.py` is configured
- `install.py` next-steps output now prints the exact `VENV_PY = _venv_python("...")` line to paste into `search_config.py`
- `install.py` next-steps output now prints the resolved venv python path for use in hook commands
- `hooks/search-first.py` gate message now uses `VENV_PY` from `search_config` instead of a hardcoded Unix path
- README restructured to professional standard: table of contents, installation flag table, configuration variable table, concrete usage examples, before/after token comparison
- README hook commands changed from `python3 .claude/hooks/...` to venv-python path (fixes Windows compatibility)
- README venv path examples now show both macOS/Linux (`bin/python`) and Windows (`Scripts\python`) variants
- README file tree corrected from `export_less_tokens/` to `less_tokens_claude/`

### Fixed
- `tools/search_config.py` docstring referenced a non-existent `less_tokens.md` file

## [0.1.0] - 2026-05-03

### Added
- `tools/embeddings.py` — crawls source files, chunks by structure (functions, headings, SQL statements), embeds with `BAAI/bge-small-en-v1.5` (384-dim), stores L2-normalized float32 vectors in SQLite
- `tools/search.py` — semantic search CLI; embeds query, computes cosine similarity, returns top-k chunks
- `tools/db.py` — SQLite helpers; WAL mode, schema init, row-count verification
- `tools/search_config.py` — single config file for venv path, indexed dirs, exclusions, and source types
- `schema/index.sql` — `documents` table with `UNIQUE(source_path, source_key)` constraint for incremental refresh
- `hooks/search-first.py` — PreToolUse hook that blocks `Read` on indexed files unless a search ran in the last 5 minutes
- `hooks/index-refresh.py` — PostToolUse hook that re-embeds in the background after `Edit`/`Write` on indexed files
- `install.py` — cross-platform installer (Windows/macOS/Linux); auto-detects venv, installs deps, initializes DB
- `.gitignore` — ignores `index.db`, `__pycache__/`, `*.pyc`, `*.pyo`, `.claude/state/`, `.DS_Store`

[Unreleased]: https://github.com/hofmockel/less_tokens_claude/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/hofmockel/less_tokens_claude/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/hofmockel/less_tokens_claude/releases/tag/v0.1.0
