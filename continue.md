# Continue: less_tokens

> **Next focus:** CP1 (order 9, P1, Research) — make compaction retention task-aware. Or, if a
> quicker win is preferred, CX31 (order 21, P1, Ready) — wire `map_bash_read` into
> `context-cache.py`, a well-scoped one-line-import fix with a clear acceptance test already.

## Current state

Working tree clean, nothing uncommitted. `BACKLOG.md` **Ready now** is empty; the **Next** table
(order 9–22) is the queue. IR1 (order 8) shipped and closed last session. HS2 (production hybrid
lexical+vector retrieval) is shipped and closed. Local `main` and `origin/main` are reconciled —
see the reconciliation note below before assuming either branch's history in isolation.

## What happened this session (2026-07-22)

- Shipped **IR1** — `.claude/tools/instruction_chain_audit.py` (`--agent claude|codex [--cwd PATH]
  [--json]`) reports the actual launch-time instruction chain (fixed vs on-demand tokens) instead
  of `claudemd_audit.py`'s single-root-file view. Verified real platform mechanics against
  Anthropic's and OpenAI's docs before building, rather than trusting the backlog item's own
  description: CLAUDE.md ancestry + recursive `.claude/rules/**/*.md` (scoped vs unscoped) +
  bounded `MEMORY.md` token count (never exposes content) for Claude; `AGENTS.override.md` →
  `AGENTS.md` → fallback-filename chain + `project_doc_max_bytes` cutoff for Codex. Flags rules
  using the community-reported-unreliable `paths:` frontmatter form (`globs:` works instead —
  unconfirmed by a maintainer, cited rather than assumed).
- The Codex shim (`.less_tokens/tools/instruction_chain_audit.py`) was generated by hand
  (`install._codex_tool_shim_text()`) instead of via `install.py --self-refresh --agent codex`,
  because local Codex (`0.145.0`) is outside CX32's verified hook window and the installer refuses
  to wire Codex adapters outside it. Re-run the installer path once CX32 widens the window, to
  confirm byte-identical shim output.
- Verification: 7 new tests (`test_instruction_chain_audit.py`), `dev.py unit` 1125 passed,
  `changelog_gate.py` clean for IR1 (pre-existing unrelated D1/HS2 gate warnings noted but not
  touched — out of scope for this session).
- Recorded as an Accepted decision in `DECISIONS.md`; `CHANGELOG.md` `[Unreleased]` entry added;
  `IR1` row and prose deleted from `BACKLOG.md`.
- Full methodology and 3 modeling assumptions not verified live (Codex byte-budget boundary
  behavior, auto-memory directory slug algorithm, the `paths:` bug itself):
  `reports/runs/2026-07-22-ir1-instruction-chain-audit/report.md`.

**Reconciliation (this session, after IR1 shipped):** local `main` and `origin/main` had
diverged from a shared base (`ffae166`) with genuinely duplicate work on both sides — HP1
close-out, the `install.py --update` pytest-suite fix, and the HS1 benchmark close-out were each
done independently on local `main` and on `origin/main` (merged there as PR #113/#114) with
different write-ups but the same effective outcome. Reconciled by rebasing local's non-duplicate
commits onto `origin/main`: **`fix: ship hybrid lexical-vector retrieval`** (the actual HS2
production code — FTS5 schema/triggers, `db.py` migration, `search.py` RRF fusion; this was
*not* a duplicate, origin only had the HS1 research benchmark and an open HS2 backlog row),
**`fix: close stale D1 recovery-guidance item`**, and **IR1**. The three duplicate local commits
(HP1 close-out, install.py fix, HS1 benchmark close-out) were dropped in favor of origin's
already-merged, reviewed versions. One CHANGELOG.md conflict during the rebase (insertion-point
context mismatch, not a real content conflict) resolved by hand. `main` is now 3 commits ahead of
`origin/main` (HS2 ship, D1 fix, IR1) and ready to push — **not yet pushed**; do that before
starting new work to avoid the same divergence recurring, especially since another session/agent
appears to be working against `origin/main` via PRs in parallel.

## Open work

See [BACKLOG.md](BACKLOG.md) **Next** table, order 9 onward. Two flagged-but-unblocked P1 items
worth prioritizing: **CP1** (compaction retention, Research) and **CX31** (Codex `context-cache.py`
gap, Ready — small fix, has a clean acceptance test). **CX32** (extend the Codex hook window past
`0.144.6`) is worth doing alongside CX31 since local Codex is already at `0.145.0` and blocking
both CX31's own live verification and IR1's shim-generation path.

## Suggested skills

- `/bugfix` — if picking CX31, it's a well-scoped bugfix-shaped fix, not a research spike.
- `/bug-hunt` — hasn't run this session; worth a sweep if picking a doc-only item (P4/P5/D2/D3/D6)
  instead of code.

## Start here

**Push `main` first** (`git push`, plain fast-forward now that it's rebased onto `origin/main`)
and confirm no one else pushed in the meantime. Then read `BACKLOG.md`'s CP1 and CX31 entries in
full (both have detailed acceptance criteria inline), pick one, and follow this repo's Research
vs Ready workflow accordingly.

---
_Last updated at HEAD `cb00aa7` (rebased onto `origin/main`, unpushed) on 2026-07-22._
