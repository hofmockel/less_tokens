# Continue: less_tokens

> **Next focus:** capture the 2 remaining `not_yet_measured` HP1 cells (`edit_verification`,
> `verbose_final_response`, both agents), or pick up **CX31** (real fix: wire `map_bash_read` into
> `context-cache.py` — well-scoped, has a clear acceptance test already). HP1's infrastructure and
> doc/CI wiring are done.

## Current state

HP1's plan (`/Users/michael/.claude/plans/validated-scribbling-wirth.md`, steps 1-10) is fully
built except for evidence coverage. `agents/common/conformance/matrix.json` has 12 of 14
workload:agent:release cells with real live-captured evidence; 2 are honestly `not_yet_measured`
(`edit_verification:{claude,codex}`, `verbose_final_response:{claude,codex}`).
`repeated_read_search:codex` was captured 2026-07-22
(`reports/runs/2026-07-22-hp1-repeated-read-search-codex/report.md`): a repeated `rg` search over
`Bash` is blocked, but a repeated whole-file `cat`/`head`/`tail`/`sed` read is not —
`context-cache.py` never calls CX25's `map_bash_read` (filed as **CX31**, a one-line-import-plus-
verification fix — the `Read`-branch logic itself is proven correct via the opt-in
`mcp__filesystem__` path). Also found: local Codex is now `0.145.0`, outside `install.py`'s
verified `0.142.3–0.144.6` window (filed as **CX32**, research).

`.claude/tools/conformance_matrix.py` (+ `.less_tokens/tools/` runpy shim) renders the matrix into
`#### Conformance matrix` sections in `README.md`/`DOCUMENTATION.md`. `README.md`'s strategy table
(via `strategy_registry.py`) links its terse-output/truncate-output/compact-trigger rows to the
matrix instead of carrying unlinked percentages. `.claude/tests/unit/test_conformance_matrix.py`
(6 cases) and a `conformance_matrix.py --check` CI step in `tests.yml` are both in place.
`DECISIONS.md` cites `install.py:do_check()` for HP1's install-health bullet. `BACKLOG.md`'s HP1
row and the `[Unreleased]` `CHANGELOG.md` entry both name the 2 outstanding cells explicitly. Full
HP1 writeup: `reports/runs/2026-07-21-hp1-conformance-matrix/report.md`.

Working tree has this session's changes uncommitted — `BACKLOG.md` (HP1 progress note + new
CX31/CX32 rows), `CHANGELOG.md` (`[Unreleased]` entry), `README.md`/`DOCUMENTATION.md`
(regenerated by `conformance_matrix.py`, not hand-edited), `agents/common/conformance/matrix.json`
(`repeated_read_search:codex:0.144.6` filled in), new
`.claude/tests/fixtures/conformance/repeated_read_search/codex/{README.md,0.144.6/*.json}`, and
new `reports/runs/2026-07-22-hp1-repeated-read-search-codex/report.md`. Commit before starting new
work (`git status`/`git diff` first).

## What happened this session (2026-07-22)

- Read `context-cache.py`'s source before writing any live probe and caught a real gap before
  guessing at behavior: the hook's mapper (`map_read_or_search`) only handles the opt-in
  `mcp__filesystem__` tool shape, never CX25's `map_bash_read` — unlike the 6 hooks CX25's fix
  list actually named. Confirmed live with 3 probes (`rg` search repeat: blocked; `cat` read
  repeat: not blocked; `mcp__filesystem__read_text_file` repeat: blocked) rather than assuming
  from source alone.
- `install.py --agent codex --dry-run` against a fresh disposable temp repo refused to wire hooks
  because local Codex (`0.145.0`) is outside the verified `0.142.3–0.144.6` window — worked around
  by invoking `context-cache.py` directly with a hand-crafted `0.144.6`-schema stdin payload
  (`LESS_TOKENS_STATE_DIR` env override), same method `indexed_whole_file_read:codex` used. Filed
  the version-window gap as CX32 rather than silently working around it forever.
- Filed CX31 (the actual fix) and CX32 (verify/extend the window) in `BACKLOG.md` instead of
  fixing CX31 inline — out of scope for a measurement-only HP1 pass; the fix is well-defined enough
  to hand off cleanly.
- Verification: `conformance_matrix.py` (regenerated docs), `pytest test_conformance_matrix.py`
  (6 passed), `label_consistency_gate.py` (ok), `changelog_gate.py` (skipped — no shipped-code
  change), `hook_parity_docs.py --check` (clean), full `pytest .claude/tests` (1208 passed).

## Open work

Only 2 `not_yet_measured` cells remain for HP1 to close for real:
1. `edit_verification` (both agents) — needs a real Edit/Write plus read-after-edit/post-edit-diff
   probe; not yet attempted.
2. `verbose_final_response` (both agents) — needs an unmodified-vs-terse Stop-response baseline
   diff; not yet attempted.

Once all 14 cells are `measured`, delete HP1's `BACKLOG.md` row per the "delete only when actually
done" rule and add a closing `CHANGELOG.md` entry. Until then, keep tightening the row rather than
deleting it.

Separately, **CX31** is now a clean, well-scoped fix ready to pick up any time (not blocked on
anything): add `map_bash_read` to `context-cache.py`'s `load_json_stdin(...)` call, verify both
the `PostToolUse` record and `PreToolUse` check see the rewritten shape, add regression coverage,
then flip `repeated_read_search:codex` to `action_enforced: true` with fresh evidence.

## Suggested skills

- `continue` — when handing off again.
- `less-tokens` — targeted exploration of the hook/budget code before extending the remaining
  workload captures or working CX31.

## Start here

Either pick `edit_verification` or `verbose_final_response` (follow the same
bounded-live-capture-plus-fixture-README pattern as the existing `.claude/tests/fixtures/conformance/`
entries, then re-run `conformance_matrix.py`), or pick up CX31's fix directly — it's fully scoped
in `BACKLOG.md` and doesn't require a live-capture session, just an import fix plus tests.

---
_Last updated at HEAD `08b8f4f`-equivalent (less_tokens `main`, `97181c8`) on 2026-07-22 — this
session's changes are uncommitted; commit before trusting this hash as current._
